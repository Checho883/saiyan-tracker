---
phase: 05-dashboard-core-habit-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dashboard/AuraGauge.tsx
  - frontend/src/components/dashboard/HeroSection.tsx
  - frontend/src/components/dashboard/MiniHero.tsx
  - frontend/src/components/dashboard/SaiyanAvatar.tsx
  - frontend/src/components/dashboard/ScouterHUD.tsx
  - frontend/src/components/dashboard/StatsPanel.tsx
  - frontend/src/components/dashboard/AttributeGrid.tsx
  - frontend/src/components/dashboard/AttributeBar.tsx
  - frontend/src/components/dashboard/DragonBallTracker.tsx
  - frontend/src/components/dashboard/StreakDisplay.tsx
  - frontend/src/hooks/useAuraProgress.ts
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08
  - DASH-09

must_haves:
  truths:
    - "Circular aura gauge shows daily completion percentage with smooth CSS transition"
    - "Tier label updates to Kaio-ken x3/x10/x20 at 50%/80%/100% thresholds"
    - "Four attribute bars display level, title, and XP fill progress in 2x2 grid"
    - "Dragon Ball tracker shows 7 slots with orange glow on collected balls"
    - "Saiyan avatar shows transformation image with fallback placeholder"
    - "Scouter HUD shows power level, form name, and progress bar to next form"
    - "Streak display shows current and best overall streak"
  artifacts:
    - path: "frontend/src/components/dashboard/AuraGauge.tsx"
      provides: "SVG circular progress ring with tier labels"
    - path: "frontend/src/components/dashboard/HeroSection.tsx"
      provides: "Full hero with avatar, scouter, and aura gauge"
    - path: "frontend/src/components/dashboard/MiniHero.tsx"
      provides: "Collapsed sticky hero with aura bar and power level"
    - path: "frontend/src/components/dashboard/AttributeGrid.tsx"
      provides: "2x2 grid of attribute stat cards"
    - path: "frontend/src/components/dashboard/DragonBallTracker.tsx"
      provides: "7-slot Dragon Ball display"
    - path: "frontend/src/components/dashboard/StatsPanel.tsx"
      provides: "Collapsible panel wrapping attributes, Dragon Balls, streaks"
    - path: "frontend/src/hooks/useAuraProgress.ts"
      provides: "Derived aura percentage and tier from todayHabits"
  key_links:
    - from: "frontend/src/components/dashboard/HeroSection.tsx"
      to: "powerStore"
      via: "usePowerStore selectors for transformation, powerLevel"
      pattern: "usePowerStore.*transformation"
    - from: "frontend/src/hooks/useAuraProgress.ts"
      to: "habitStore.todayHabits"
      via: "useHabitStore selector computing completion %"
      pattern: "useHabitStore.*todayHabits"
    - from: "frontend/src/components/dashboard/AttributeGrid.tsx"
      to: "powerStore.attributes"
      via: "usePowerStore selector for AttributeDetail[]"
      pattern: "usePowerStore.*attributes"
    - from: "frontend/src/components/dashboard/StatsPanel.tsx"
      to: "habitStore.todayHabits"
      via: "useHabitStore selector for max streak_current/streak_best passed as props to StreakDisplay"
      pattern: "useHabitStore.*todayHabits"
---

<objective>
Build the hero section (avatar + scouter HUD + circular aura gauge with collapsing behavior) and the game-stats panel (attribute bars, Dragon Ball tracker, streak display) as composable components.

Purpose: The hero section is the visual centerpiece showing transformation and daily progress. Game stats give the player a full picture of their RPG progression. Both are pure display components consuming existing store data.
Output: HeroSection, MiniHero, AuraGauge, SaiyanAvatar, ScouterHUD, StatsPanel, AttributeGrid, AttributeBar, DragonBallTracker, StreakDisplay, useAuraProgress hook
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-core-habit-management/05-RESEARCH.md

<interfaces>
<!-- Key types from existing codebase -->

From frontend/src/types/index.ts:
```typescript
export type Attribute = 'str' | 'vit' | 'int' | 'ki';
export interface AttributeDetail {
  attribute: Attribute;
  raw_xp: number;
  level: number;
  title: string | null;
  xp_for_current_level: number;
  xp_for_next_level: number;
  progress_percent: number;
}
export interface PowerResponse {
  power_level: number;
  transformation: string;
  transformation_name: string;
  next_transformation: string | null;
  next_threshold: number | null;
  dragon_balls_collected: number;
  attributes: AttributeDetail[];
}
export interface HabitTodayResponse extends HabitResponse {
  completed: boolean;
  streak_current: number;
  streak_best: number;
}
```

From frontend/src/store/powerStore.ts:
```typescript
interface PowerState {
  powerLevel: number;
  transformation: string;
  transformationName: string;
  nextTransformation: string | null;
  nextThreshold: number | null;
  dragonBallsCollected: number;
  attributes: AttributeDetail[];
}
export const usePowerStore = create<PowerState>(...);
```

From frontend/src/store/habitStore.ts:
```typescript
interface HabitState {
  todayHabits: HabitTodayResponse[];
}
export const useHabitStore = create<HabitState>(...);
```

Tailwind tokens: attr-str (#ef4444 red), attr-vit (#22c55e green), attr-int (#3b82f6 blue), attr-ki (#f59e0b amber), saiyan-500 (orange), aura-500 (blue), warning (amber for Dragon Balls), space-700/800/900 (backgrounds)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AuraGauge, SaiyanAvatar, ScouterHUD, HeroSection, MiniHero, and useAuraProgress hook</name>
  <files>
    frontend/src/hooks/useAuraProgress.ts
    frontend/src/components/dashboard/AuraGauge.tsx
    frontend/src/components/dashboard/SaiyanAvatar.tsx
    frontend/src/components/dashboard/ScouterHUD.tsx
    frontend/src/components/dashboard/HeroSection.tsx
    frontend/src/components/dashboard/MiniHero.tsx
  </files>
  <behavior>
    - useAuraProgress returns { percent, tier, completed, due } computed from todayHabits
    - AuraGauge renders SVG circle with stroke-dashoffset proportional to percent, tier label text
    - SaiyanAvatar renders transformation image with fallback placeholder
    - ScouterHUD shows power level number, form name, progress bar to next form
    - HeroSection composes avatar + aura gauge + scouter HUD
    - MiniHero shows compact aura bar (linear) + power level for sticky header
  </behavior>
  <action>
**useAuraProgress.ts:**
- Import `useHabitStore` with selector for `todayHabits`
- Compute: `due = todayHabits.length`, `completed = todayHabits.filter(h => h.completed).length`, `percent = due > 0 ? Math.round((completed / due) * 100) : 0`
- Tier logic: `>= 100` → `'kaioken_x20'`, `>= 80` → `'kaioken_x10'`, `>= 50` → `'kaioken_x3'`, else `'base'`
- Return `{ percent, tier, completed, due }`

**AuraGauge.tsx:**
- Props: `{ percent: number; tier: string; size?: number }` (default size=140)
- SVG with viewBox `0 0 {size} {size}`, two `<circle>` elements:
  - Track circle: `fill="none"`, `stroke="var(--color-space-700)"`, `strokeWidth={4}`
  - Progress circle: `fill="none"`, `stroke` varies by tier (base: `var(--color-saiyan-500)`, kaioken_x3: `var(--color-saiyan-500)`, kaioken_x10: `var(--color-aura-500)`, kaioken_x20: `var(--color-success)`), `strokeLinecap="round"`, `strokeDasharray={circumference}`, `strokeDashoffset={offset}`, CSS `transition: stroke-dashoffset 700ms ease-out`
  - SVG rotated -90deg so progress starts from top
- Center text group (not rotated): `{percent}%` large, tier label small below
- Tier label map: `{ base: '', kaioken_x3: 'Kaio-ken x3', kaioken_x10: 'Kaio-ken x10', kaioken_x20: 'Kaio-ken x20' }`

**SaiyanAvatar.tsx:**
- Props: `{ transformation: string; className?: string }`
- Avatar image map: transformation key → `/assets/avatars/{key}.webp`
- Fallback: `onError` handler swaps to `/assets/avatars/base.webp`
- If no image loads at all, show a `div` with gradient background and lucide `User` icon
- Container with rounded-full border, aura glow shadow (box-shadow with transformation-appropriate color -- orange for base/ssj, blue for ssb/ssg, silver for ui)

**ScouterHUD.tsx:**
- Props: `{ powerLevel: number; transformationName: string; nextTransformation: string | null; nextThreshold: number | null }`
- Scouter-style layout:
  - Power level number in large monospace font with saiyan-500 color
  - Form name text (e.g., "Super Saiyan") below in text-secondary
  - If nextThreshold: progress bar showing `powerLevel / nextThreshold` as a slim horizontal bar with aura-500 fill
  - Next form label in text-muted ("Next: {nextTransformation}" or "MAX POWER" if null)

**HeroSection.tsx:**
- Imports: `usePowerStore` (useShallow for powerLevel, transformation, transformationName, nextTransformation, nextThreshold), `useAuraProgress`
- Layout: centered flex column with:
  - SaiyanAvatar (large, centered) with AuraGauge overlaid/positioned around it
  - ScouterHUD below avatar
- Background: subtle gradient from space-800 to transparent at bottom
- Padding: px-4 py-6

**MiniHero.tsx:**
- Compact version for sticky header when hero scrolls out
- Imports: `usePowerStore` (powerLevel), `useAuraProgress`
- Layout: single row with:
  - Linear progress bar (thin, aura-500 fill on space-700 track) showing aura percent
  - Power level number (compact, saiyan-500 monospace)
  - Tier label text (if not base)
- Height: h-12, background: bg-space-800 with border-b border-space-700
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/aura-gauge.test.tsx src/__tests__/hero-section.test.tsx --reporter=verbose</automated>
  </verify>
  <done>AuraGauge renders circular progress with correct tier labels at threshold percentages. SaiyanAvatar renders transformation image with fallback. ScouterHUD shows power level, form name, and next-form progress. HeroSection and MiniHero compose these sub-components. All aura-gauge and hero-section tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AttributeGrid, AttributeBar, DragonBallTracker, StreakDisplay, and StatsPanel</name>
  <files>
    frontend/src/components/dashboard/AttributeGrid.tsx
    frontend/src/components/dashboard/AttributeBar.tsx
    frontend/src/components/dashboard/DragonBallTracker.tsx
    frontend/src/components/dashboard/StreakDisplay.tsx
    frontend/src/components/dashboard/StatsPanel.tsx
  </files>
  <behavior>
    - AttributeGrid renders 2x2 grid of AttributeBar components from powerStore.attributes
    - AttributeBar shows attribute icon, level number, and XP fill bar with attribute color
    - DragonBallTracker shows 7 circles, collected ones glow orange, empty are dim outlines
    - StreakDisplay shows current and best overall streak with fire emoji
    - StatsPanel wraps all three in a collapsible panel with toggle
  </behavior>
  <action>
**AttributeBar.tsx:**
- Props: `{ detail: AttributeDetail }`
- Attribute icon map using lucide-react: `{ str: Swords, vit: Heart, int: Brain, ki: Flame }`
- Attribute color map: `{ str: 'bg-attr-str', vit: 'bg-attr-vit', int: 'bg-attr-int', ki: 'bg-attr-ki' }` and text variants
- Layout: card (bg-space-700, rounded-lg, p-3) with:
  - Top: icon + attribute name (uppercase, small) + level number (large, bold, attribute-colored)
  - Bottom: XP progress bar (slim, attribute-colored fill), width `{detail.progress_percent}%`, CSS transition on width
  - Below bar: `{detail.xp_for_current_level} / {detail.xp_for_next_level} XP` in text-muted, text-xs

**AttributeGrid.tsx:**
- Import `usePowerStore` with selector for `attributes`
- Render 2x2 grid (`grid grid-cols-2 gap-3`) of `<AttributeBar>` components
- Grid order: STR, VIT, INT, KI (by convention, but use array order from store)

**DragonBallTracker.tsx:**
- Props: `{ collected: number }` (0-7)
- Render 7 circles in a flex row (gap-2, justify-center)
- Each circle: `w-8 h-8 rounded-full border-2 flex items-center justify-center text-xs font-bold`
- Collected (i < collected): `bg-warning border-warning text-space-900 shadow-[0_0_8px_var(--color-warning)]` — shows star count `{i+1}★`
- Empty (i >= collected): `border-space-600 text-space-600` — shows star count dimmed
- Transition on shadow/color for when a new ball is collected (CSS transition-all)

**StreakDisplay.tsx:**
- Import `useHabitStore` with selector for `todayHabits`
- Derive overall streak from habits: take max `streak_current` and max `streak_best` across all habits (approximation for display — actual overall streak comes from daily_log in check response, but for dashboard display we use the max per-habit streak)
- Or: props-based `{ currentStreak: number; bestStreak: number }` for flexibility
- Go with props-based approach, let parent compute
- Layout: flex row, "🔥 {current}" in text-lg font-bold text-saiyan-500, "Best: {best}" in text-sm text-text-secondary
- If current >= best, add subtle glow or "NEW BEST!" badge in text-xs text-success

**StatsPanel.tsx:**
- Import `usePowerStore` (useShallow for dragonBallsCollected, attributes)
- Local state: `isExpanded` (boolean, default true)
- Toggle button: row with "Stats" label, chevron icon (ChevronDown/ChevronUp from lucide), tap to toggle
- When expanded, render in a flex column (gap-4):
  1. `<AttributeGrid />`
  2. `<DragonBallTracker collected={dragonBallsCollected} />`
  3. `<StreakDisplay currentStreak={...} bestStreak={...} />`
- Collapse/expand with CSS `overflow-hidden` + `max-height` transition (300ms ease) or conditional render
- Background: bg-space-800/50, rounded-xl, px-4 py-3
- Compute streak values for StreakDisplay: derive from habitStore.todayHabits (max streak_current, max streak_best)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/game-stats.test.tsx --reporter=verbose</automated>
  </verify>
  <done>AttributeGrid renders 4 attribute cards in 2x2 grid with levels and progress bars. DragonBallTracker shows correct filled/empty slots with glow effects. StreakDisplay shows current/best streak. StatsPanel collapses/expands. All game-stats tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/__tests__/aura-gauge.test.tsx src/__tests__/game-stats.test.tsx src/__tests__/hero-section.test.tsx --reporter=verbose` — all test groups pass
- AuraGauge uses SVG circle (no external library)
- No framer-motion or motion import anywhere — CSS transitions only
- Components consume store data via selectors, no prop-drilling through Dashboard
- Dragon Ball circles use `var(--color-warning)` for orange glow
</verification>

<success_criteria>
1. Circular aura gauge renders at correct percentage with smooth CSS transition
2. Tier label shows correct Kaio-ken level at 50%/80%/100% thresholds
3. Four attribute bars display in 2x2 grid with level, progress, and attribute color
4. Dragon Ball tracker shows 7 circles with glowing orange filled balls and dim empty slots
5. Saiyan avatar renders with transformation-appropriate styling and fallback
6. Scouter HUD shows power level, form name, and next-form progress bar
7. Stats panel collapses/expands with toggle
8. All aura-gauge, game-stats, and hero-section tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-core-habit-management/05-02-SUMMARY.md`
</output>
