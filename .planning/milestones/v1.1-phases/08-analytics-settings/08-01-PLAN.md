---
phase: 08-analytics-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/hooks/useAnalyticsData.ts
  - frontend/src/components/analytics/CalendarHeatmap.tsx
  - frontend/src/components/analytics/PeriodSelector.tsx
  - frontend/src/components/analytics/StatCards.tsx
  - frontend/src/components/analytics/AttributeChart.tsx
  - frontend/src/components/analytics/PowerLevelChart.tsx
  - frontend/src/pages/Analytics.tsx
  - frontend/src/__tests__/calendar-heatmap.test.tsx
  - frontend/src/__tests__/analytics-charts.test.tsx
  - frontend/src/__tests__/stat-cards.test.tsx
autonomous: true
requirements:
  - ANLYT-01
  - ANLYT-02
  - ANLYT-03
  - ANLYT-04
  - ANLYT-05

must_haves:
  truths:
    - "Calendar heatmap displays days with gold (100%), blue (75-99%), red (50-74%), gray (<50%) color coding and blue outline for off-days"
    - "User can navigate calendar by month with prev/next controls and see correct days for each month"
    - "Attribute progression area chart shows STR/VIT/INT/KI growth over time with period selector (week/month/all)"
    - "Summary stat cards display perfect days count, average %, total XP, and longest streak in Scouter HUD style"
    - "Power level line chart shows cumulative XP progression over days"
  artifacts:
    - path: "frontend/src/hooks/useAnalyticsData.ts"
      provides: "Data fetching hook for analytics page — summary, calendar, and chart data"
    - path: "frontend/src/components/analytics/CalendarHeatmap.tsx"
      provides: "SVG month-grid calendar with 4-color coding and off-day blue outlines"
    - path: "frontend/src/components/analytics/PeriodSelector.tsx"
      provides: "Segmented control for week/month/all period selection"
    - path: "frontend/src/components/analytics/StatCards.tsx"
      provides: "Scouter HUD styled stat cards for perfect days, avg %, total XP, longest streak"
    - path: "frontend/src/components/analytics/AttributeChart.tsx"
      provides: "Recharts stacked area chart with neon glow for STR/VIT/INT/KI"
    - path: "frontend/src/components/analytics/PowerLevelChart.tsx"
      provides: "Recharts line chart showing cumulative XP progression"
    - path: "frontend/src/pages/Analytics.tsx"
      provides: "Full analytics page composing all chart/card components"
  key_links:
    - from: "frontend/src/hooks/useAnalyticsData.ts"
      to: "frontend/src/services/api.ts"
      via: "Calls analyticsApi.summary(), habitsApi.calendarAll(), and derives chart data"
      pattern: "analyticsApi|habitsApi\\.calendarAll"
    - from: "frontend/src/pages/Analytics.tsx"
      to: "frontend/src/hooks/useAnalyticsData.ts"
      via: "Uses useAnalyticsData hook with period and month state"
      pattern: "useAnalyticsData"
---

<objective>
Install recharts, create the full Analytics page with calendar heatmap, attribute progression area chart, power level line chart, summary stat cards, and period selector.

Purpose: Users need visual analytics to review their habit history — the calendar heatmap gives daily overview, charts show progression trends, and stat cards provide key metrics. This is the data visualization layer that makes XP and habit completion feel tangible.
Output: useAnalyticsData.ts, CalendarHeatmap.tsx, PeriodSelector.tsx, StatCards.tsx, AttributeChart.tsx, PowerLevelChart.tsx, Analytics.tsx page, test files
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-analytics-settings/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/types/index.ts:
```typescript
export type AnalyticsPeriod = 'week' | 'month' | 'all';

export interface AnalyticsSummary {
  perfect_days: number;
  avg_completion: number;
  total_xp: number;
  days_tracked: number;
  longest_streak: number;
}

export interface CalendarDay {
  date: string;
  is_perfect_day: boolean;
  completion_tier: string; // 'gold' | 'silver' | 'bronze' | 'base'
  xp_earned: number;
  is_off_day: boolean;
}
```

From frontend/src/services/api.ts:
```typescript
export const habitsApi = {
  calendarAll: (month: string) => api.get('habits/calendar/all', { searchParams: { month } }).json<CalendarDay[]>(),
};

export const analyticsApi = {
  summary: (period?: 'week' | 'month' | 'all') =>
    api.get('analytics/summary', { searchParams: period ? { period } : {} }).json<AnalyticsSummary>(),
};
```

From frontend/src/store/powerStore.ts (attributes data):
```typescript
interface PowerState {
  powerLevel: number;
  attributes: AttributeDetail[];
  fetchPower: () => Promise<void>;
}
// AttributeDetail has: attribute, raw_xp, level, title, progress_percent
```

From frontend/src/components/dashboard/ScouterHUD.tsx (visual reference):
```typescript
// Uses: font-mono, text-saiyan-500, text-3xl for power level display
// Angular style with progress bar
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install recharts, create useAnalyticsData hook, PeriodSelector, and CalendarHeatmap with month navigation</name>
  <files>
    frontend/package.json,
    frontend/src/hooks/useAnalyticsData.ts,
    frontend/src/components/analytics/PeriodSelector.tsx,
    frontend/src/components/analytics/CalendarHeatmap.tsx,
    frontend/src/__tests__/calendar-heatmap.test.tsx
  </files>
  <behavior>
    - useAnalyticsData(period, month) returns { summary, calendarDays, isLoading } — fetches analyticsApi.summary(period) and habitsApi.calendarAll(month)
    - PeriodSelector renders 3 segmented buttons (Week/Month/All) with active state highlight in saiyan-500
    - CalendarHeatmap renders SVG month grid with 7 columns (Sun-Sat), correct row count per month
    - CalendarHeatmap colors: gold (#EAB308) for completion_tier 'gold', blue (#3B82F6) for 'silver', red (#EF4444) for 'bronze', gray (#4B5563) for 'base' or no data
    - Off-day cells have 2px blue (#3B82F6) stroke outline
    - Month navigation: prev/next arrow buttons update displayed month
    - Day click shows tooltip with completion %, XP earned
  </behavior>
  <action>
    1. Install recharts: `cd frontend && npm install recharts`
    2. Add react-is override to package.json — add `"overrides": { "react-is": "^19.0.0" }` at root level of package.json. Then run `npm install` again to apply the override.
    3. Create `frontend/src/hooks/useAnalyticsData.ts`:
       - Import `useState, useEffect` from react
       - Import `analyticsApi, habitsApi` from '../services/api'
       - Import `AnalyticsSummary, CalendarDay, AnalyticsPeriod` from types
       - Hook signature: `useAnalyticsData(period: AnalyticsPeriod, month: string)`
       - `month` is format `YYYY-MM` (e.g., '2026-03')
       - State: `summary: AnalyticsSummary | null`, `calendarDays: CalendarDay[]`, `isLoading: boolean`
       - useEffect on `period`: fetch `analyticsApi.summary(period)`, update summary
       - useEffect on `month`: fetch `habitsApi.calendarAll(month)`, update calendarDays
       - Return `{ summary, calendarDays, isLoading }`

    4. Create `frontend/src/components/analytics/PeriodSelector.tsx`:
       - Props: `period: AnalyticsPeriod`, `onChange: (p: AnalyticsPeriod) => void`
       - Render 3 buttons in a flex row with rounded-lg container bg-space-700
       - Each button: text 'Week' | 'Month' | 'All', active gets bg-saiyan-500 text-white, inactive gets text-text-secondary
       - Transition colors on hover/active
       - Use `role="group"` and `aria-label="Period selector"` for accessibility

    5. Create `frontend/src/components/analytics/CalendarHeatmap.tsx`:
       - Props: `days: CalendarDay[]`, `month: string`, `onPrev: () => void`, `onNext: () => void`
       - Parse month string to get year/month numbers
       - Calculate `daysInMonth` and `firstDayOfWeek` (0=Sunday)
       - Render header: `<` button, month/year text, `>` button using ChevronLeft/ChevronRight from lucide-react
       - Render weekday labels: Su, Mo, Tu, We, Th, Fr, Sa
       - Render grid of day cells as div elements (NOT SVG — divs work better for tooltips and interaction):
         - 7 columns using CSS grid (`grid-cols-7`)
         - Empty cells for days before first of month
         - Each day cell: 36x36px rounded-md
         - Color mapping function: `colorForTier(tier)`:
           - 'gold' -> 'bg-yellow-500' (100% completion)
           - 'silver' -> 'bg-blue-500' (75-99%)
           - 'bronze' -> 'bg-red-500' (50-74%)
           - 'base' or missing -> 'bg-gray-600' (<50%)
         - Off-day cells: add `ring-2 ring-blue-500` for blue outline
         - Day number text inside cell: text-xs centered
       - Tooltip on click/tap: show popover with date, completion %, XP earned
         - Use simple state-based popover (no tooltip library needed)
         - Position below the clicked cell
         - Style: bg-space-700 rounded-lg shadow-xl p-3 text-sm

    6. Create `frontend/src/__tests__/calendar-heatmap.test.tsx`:
       - Test: "renders correct number of day cells for month"
       - Test: "applies gold color for 100% completion tier"
       - Test: "applies blue outline for off-days"
       - Test: "prev/next buttons call navigation callbacks"
       - Test: "shows tooltip on day click with completion info"
       - Mock the data: provide CalendarDay[] array with known tiers

    7. Run `cd frontend && npx vitest run` to verify all tests pass
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - recharts installed with react-is override
    - useAnalyticsData hook fetches summary and calendar data
    - PeriodSelector renders week/month/all segmented control
    - CalendarHeatmap renders month grid with 4-color coding and off-day outlines
    - Month navigation via prev/next buttons works
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StatCards, AttributeChart, PowerLevelChart, and compose full Analytics page</name>
  <files>
    frontend/src/components/analytics/StatCards.tsx,
    frontend/src/components/analytics/AttributeChart.tsx,
    frontend/src/components/analytics/PowerLevelChart.tsx,
    frontend/src/pages/Analytics.tsx,
    frontend/src/__tests__/stat-cards.test.tsx,
    frontend/src/__tests__/analytics-charts.test.tsx
  </files>
  <behavior>
    - StatCards renders 4 Scouter HUD styled cards: Perfect Days (count), Average (%), Total XP (number), Longest Streak (days)
    - StatCards use font-mono, tabular-nums, angular clip-path, scan-line animation matching ScouterHUD style
    - AttributeChart renders recharts AreaChart with 4 stacked areas (STR red, VIT green, INT blue, KI yellow) with neon glow SVG filter
    - PowerLevelChart renders recharts LineChart showing cumulative XP over time with neon glow
    - Analytics page composes all components: header, PeriodSelector, StatCards grid, CalendarHeatmap, charts
    - Charts animate on initial load with progressive draw-in (recharts animationDuration)
  </behavior>
  <action>
    1. Create `frontend/src/components/analytics/StatCards.tsx`:
       - Props: `summary: AnalyticsSummary | null`
       - Render 2x2 grid of stat cards (`grid-cols-2 gap-3`)
       - Each card: `bg-space-800 border border-saiyan-500/30 rounded-lg p-3 relative overflow-hidden`
       - Add angular clip-path via CSS: `clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 12px 100%, 0 calc(100% - 12px))` — matching Scouter angular aesthetic
       - Scan-line overlay: `absolute inset-0 bg-gradient-to-b from-saiyan-500/5 via-transparent to-transparent h-[200%] animate-[scan_3s_linear_infinite]`
       - Add `@keyframes scan { from { transform: translateY(-50%); } to { transform: translateY(0); } }` via inline style or Tailwind arbitrary
       - Label: `text-xs text-text-muted uppercase tracking-wider`
       - Value: `text-2xl font-mono font-bold text-saiyan-400 tabular-nums`
       - Cards: Perfect Days (summary.perfect_days), Average (Math.round(summary.avg_completion * 100) + '%'), Total XP (summary.total_xp.toLocaleString()), Streak (summary.longest_streak + ' days')
       - When summary is null: show loading skeleton (bg-space-700 animate-pulse bars)

    2. Create `frontend/src/components/analytics/AttributeChart.tsx`:
       - Props: `calendarDays: CalendarDay[]`
       - NOTE on data: Backend does not store per-attribute daily XP. Use total daily XP from calendarDays to show XP progression.
       - Transform data: map calendarDays to `{ date: string, xp: number }[]` sorted by date
       - For attribute visualization: use `usePowerStore` to get current attribute levels and render a STATIC attribute comparison bar below the time-series chart
       - Render `ResponsiveContainer` wrapping `AreaChart`:
         - Height: 200px
         - `<defs>`: SVG filter for neon glow (`feGaussianBlur stdDeviation="2.5"` + `feMerge`), gradient fills
         - `<XAxis>` with dataKey="date", tick fill text-muted, fontSize 11, tickFormatter to show day only
         - `<YAxis>` with tick fill text-muted, fontSize 11
         - `<Tooltip>` with dark styled content (bg-space-700 border-space-600)
         - Single `<Area>` for total XP: type="monotone", stroke="#3B82F6" (blue), fill with gradient, strokeWidth 2, filter="url(#neonGlow)", animationDuration 1500
       - Below the chart, render attribute level bars:
         - Import `usePowerStore` for current attributes
         - 4 horizontal bars: STR (red-500), VIT (green-500), INT (blue-500), KI (yellow-500)
         - Each shows: label, level number, progress bar fill

    3. Create `frontend/src/components/analytics/PowerLevelChart.tsx`:
       - Props: `calendarDays: CalendarDay[]`
       - Transform data: compute cumulative XP sum (running total of xp_earned sorted by date)
       - Render `ResponsiveContainer` wrapping `LineChart`:
         - Height: 200px
         - Same neon glow filter as AttributeChart
         - `<XAxis>` date labels, `<YAxis>` XP values
         - `<Tooltip>` dark styled
         - `<Line>` type="monotone", dataKey="cumulativeXp", stroke="#EAB308" (gold), strokeWidth 2, dot={false}, filter="url(#neonGlow)", animationDuration 1500
       - Label: "Power Level Progression"

    4. Update `frontend/src/pages/Analytics.tsx`:
       - Import all analytics components and useAnalyticsData hook
       - State: `period` (AnalyticsPeriod, default 'all'), `month` (string, default current YYYY-MM)
       - Month navigation helpers: `prevMonth()` and `nextMonth()` that decrement/increment the month string
       - Call `useAnalyticsData(period, month)`
       - Layout (vertical scroll, p-4, space-y-6):
         - Header: `<h1>` "Analytics" in text-saiyan-500
         - `<PeriodSelector period={period} onChange={setPeriod} />`
         - `<StatCards summary={summary} />`
         - `<CalendarHeatmap days={calendarDays} month={month} onPrev={prevMonth} onNext={nextMonth} />`
         - Section header "XP Over Time"
         - `<AttributeChart calendarDays={calendarDays} />`
         - Section header "Power Level"
         - `<PowerLevelChart calendarDays={calendarDays} />`
       - Add bottom padding (`pb-24`) to clear the BottomTabBar

    5. Create `frontend/src/__tests__/stat-cards.test.tsx`:
       - Test: "renders all 4 stat cards with correct values"
       - Test: "shows loading skeleton when summary is null"
       - Test: "formats XP with locale string"

    6. Create `frontend/src/__tests__/analytics-charts.test.tsx`:
       - Mock recharts components (ResponsiveContainer, AreaChart, etc.) to avoid JSDOM rendering issues
       - Test: "AttributeChart renders without crashing with data"
       - Test: "PowerLevelChart renders without crashing with data"
       - Test: "Analytics page renders all sections"

    7. Run `cd frontend && npx vitest run` to verify all tests pass
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - StatCards render 4 Scouter HUD styled cards with angular clip-path and scan-line effect
    - AttributeChart renders area chart with neon glow and attribute level bars
    - PowerLevelChart renders cumulative XP line chart with neon glow
    - Analytics page composes all components with header, period selector, stat cards, calendar, charts
    - Charts animate on load with progressive draw-in
    - All tests pass including new stat-cards and analytics-charts tests
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` — all tests pass including new analytics tests
- Analytics page renders with calendar heatmap, stat cards, and charts
- Calendar heatmap shows 4-color coding with off-day blue outlines
- Month navigation works via prev/next controls
- Period selector switches between week/month/all and updates stat cards
- Charts show neon glow effect on dark background
- Stat cards match ScouterHUD angular aesthetic
</verification>

<success_criteria>
- [ ] recharts installed with react-is override for React 19 compatibility
- [ ] Calendar heatmap renders month grid with gold/blue/red/gray color coding
- [ ] Off-day cells show blue outline
- [ ] Month navigation via prev/next controls updates calendar data
- [ ] Period selector (week/month/all) updates summary stats
- [ ] Stat cards display: perfect days, average %, total XP, longest streak
- [ ] Stat cards have Scouter HUD style (angular, mono-spaced, scan-line)
- [ ] Area chart shows XP progression with neon glow effect
- [ ] Line chart shows cumulative power level with neon glow
- [ ] Charts animate on initial load
- [ ] All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics-settings/08-01-SUMMARY.md`
</output>
