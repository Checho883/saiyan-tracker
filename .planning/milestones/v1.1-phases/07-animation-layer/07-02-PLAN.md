---
phase: 07-animation-layer
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/animations/AnimationPlayer.tsx
  - frontend/src/components/animations/PerfectDayOverlay.tsx
  - frontend/src/components/animations/CapsuleDropOverlay.tsx
  - frontend/src/components/animations/DragonBallTrajectory.tsx
  - frontend/src/components/animations/TransformationOverlay.tsx
  - frontend/src/components/animations/ShenronCeremony.tsx
  - frontend/src/components/dashboard/DragonBallTracker.tsx
  - frontend/src/components/dashboard/HeroSection.tsx
  - frontend/src/__tests__/perfect-day.test.tsx
  - frontend/src/__tests__/capsule-drop.test.tsx
  - frontend/src/__tests__/dragon-ball-trajectory.test.tsx
  - frontend/src/__tests__/transformation.test.tsx
  - frontend/src/__tests__/shenron-ceremony.test.tsx
autonomous: true
requirements:
  - ANIM-03
  - ANIM-04
  - ANIM-05
  - ANIM-06
  - ANIM-07
  - ANIM-08
  - ANIM-09

must_haves:
  truths:
    - "Perfect Day overlay plays a ~2.5s choreographed sequence: dark overlay, screen shake, particle burst, '100% COMPLETE' text scales in, XP counter ticks up, character quote fades in, everything fades out"
    - "Capsule drop overlay bounces in center-screen with scale spring, pulses invitingly; tap triggers 3D card flip (rotateY) revealing reward with rarity glow (white common, blue rare, purple epic)"
    - "Dragon Ball earned flies from center-screen in an arc trajectory toward the tracker slot position"
    - "Transformation overlay plays form-specific visual: golden flash (SSJ), lightning (SSJ2), red aura (SSG), blue aura (SSB), silver flash (UI) — each includes avatar swap to new form"
    - "Shenron ceremony plays full-screen sequence: sky darkens, lightning, Shenron scales up, wish text, balls scatter; enforces minimum 1 active wish before allowing completion"
    - "All full-screen sequences support tap-to-skip after first 1 second"
    - "AnimationPlayer dispatches to the correct overlay component based on event type"
  artifacts:
    - path: "frontend/src/components/animations/PerfectDayOverlay.tsx"
      provides: "Full-screen Perfect Day celebration with shake, particles, text, counter, quote"
    - path: "frontend/src/components/animations/CapsuleDropOverlay.tsx"
      provides: "Capsule bounce-in, pulse, tap-to-flip, 3D card reveal with rarity glow"
    - path: "frontend/src/components/animations/DragonBallTrajectory.tsx"
      provides: "Dragon Ball arc animation from center to tracker"
    - path: "frontend/src/components/animations/TransformationOverlay.tsx"
      provides: "Form-specific transformation visual with color flash and avatar swap"
    - path: "frontend/src/components/animations/ShenronCeremony.tsx"
      provides: "Full Shenron summoning ceremony with wish enforcement"
  key_links:
    - from: "frontend/src/components/animations/AnimationPlayer.tsx"
      to: "frontend/src/components/animations/PerfectDayOverlay.tsx"
      via: "Switch on event.type === 'perfect_day'"
      pattern: "PerfectDayOverlay"
    - from: "frontend/src/components/animations/AnimationPlayer.tsx"
      to: "frontend/src/components/animations/CapsuleDropOverlay.tsx"
      via: "Switch on event.type === 'capsule_drop'"
      pattern: "CapsuleDropOverlay"
    - from: "frontend/src/components/animations/AnimationPlayer.tsx"
      to: "frontend/src/components/animations/ShenronCeremony.tsx"
      via: "Switch on event.type === 'shenron'"
      pattern: "ShenronCeremony"
    - from: "frontend/src/components/animations/ShenronCeremony.tsx"
      to: "frontend/src/store/rewardStore.ts"
      via: "Reads wishes to enforce minimum 1 active wish before completion"
      pattern: "useRewardStore.*wishes"
---

<objective>
Build all five full-screen animation overlays (Perfect Day, Capsule Drop/Reveal, Dragon Ball Trajectory, Transformation, Shenron Ceremony) and wire them into the AnimationPlayer dispatcher. Each overlay is a self-contained component with choreographed sequences, tap-to-skip after 1s, and proper exit animations.

Purpose: These overlays ARE the dopamine payoff — the choreographed moments that make the app feel like a game. Without them, habit checks trigger sounds but nothing visual beyond the inline tier banner.
Output: 5 overlay components, updated AnimationPlayer with event dispatching, test files
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-animation-layer/07-RESEARCH.md
@.planning/phases/07-animation-layer/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 -->

From frontend/src/components/animations/springs.ts:
```typescript
export const SPRINGS = {
  bouncy: { type: 'spring' as const, stiffness: 400, damping: 15 },
  gentle: { type: 'spring' as const, stiffness: 200, damping: 20 },
  snappy: { type: 'spring' as const, stiffness: 500, damping: 25 },
  wobbly: { type: 'spring' as const, stiffness: 300, damping: 10 },
} as const;
```

From frontend/src/components/animations/useSkippable.ts:
```typescript
export function useSkippable(minDurationMs: number, onSkip: () => void): { canSkip: boolean; skip?: () => void };
```

From frontend/src/components/animations/ScreenShake.tsx:
```typescript
export function ScreenShake({ children, trigger }: { children: React.ReactNode; trigger: boolean }): JSX.Element;
```

From frontend/src/components/animations/ParticleBurst.tsx:
```typescript
export function ParticleBurst({ count, origin, color, onComplete }: ParticleBurstProps): JSX.Element;
```

From frontend/src/store/uiStore.ts:
```typescript
export type AnimationEvent =
  | { type: 'tier_change'; tier: string }
  | { type: 'perfect_day' }
  | { type: 'capsule_drop'; rewardTitle: string; rarity: string }
  | { type: 'dragon_ball'; count: number }
  | { type: 'transformation'; form: string; name: string }
  | { type: 'xp_popup'; amount: number; attribute: string }
  | { type: 'shenron' };
```

From frontend/src/store/rewardStore.ts:
```typescript
interface RewardState {
  wishes: WishResponse[];
  fetchWishes: () => Promise<void>;
}
// WishResponse has: id, title, is_active, times_wished
```

From frontend/src/components/dashboard/SaiyanAvatar.tsx:
```typescript
export function SaiyanAvatar({ transformation, className }: { transformation: string; className?: string }): JSX.Element;
```

From frontend/src/components/dashboard/DragonBallTracker.tsx:
```typescript
export function DragonBallTracker({ collected }: { collected: number }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PerfectDayOverlay, CapsuleDropOverlay, and DragonBallTrajectory components</name>
  <files>
    frontend/src/components/animations/PerfectDayOverlay.tsx,
    frontend/src/components/animations/CapsuleDropOverlay.tsx,
    frontend/src/components/animations/DragonBallTrajectory.tsx,
    frontend/src/components/dashboard/DragonBallTracker.tsx,
    frontend/src/__tests__/perfect-day.test.tsx,
    frontend/src/__tests__/capsule-drop.test.tsx,
    frontend/src/__tests__/dragon-ball-trajectory.test.tsx
  </files>
  <behavior>
    - PerfectDayOverlay: renders full-screen overlay, shows "100% COMPLETE" text, supports tap-to-skip after 1s, calls onComplete when sequence ends
    - CapsuleDropOverlay: shows capsule bouncing in with scale spring, pulse animation, tap flips card (rotateY 180), reveals reward title and rarity glow, auto-dismisses after 4s or tap after 1.5s
    - DragonBallTrajectory: renders a golden ball that arcs from center to upper portion of screen, calls onComplete after animation
    - All overlays accept onComplete callback and have motion.div with initial/animate/exit props
  </behavior>
  <action>
    1. Create `frontend/src/components/animations/PerfectDayOverlay.tsx`:
       - Props: `onComplete: () => void`
       - Import ScreenShake, ParticleBurst, useSkippable, SPRINGS from sibling files
       - Choreography using sequential state transitions via useEffect chain:
         a. Mount: dark overlay fades in (motion.div opacity 0->1, bg-black/80)
         b. 200ms: trigger ScreenShake (set shaking=true)
         c. 500ms: trigger ParticleBurst from center of viewport
         d. 700ms: "100% COMPLETE" text scales in (motion.div scale 0->1 with SPRINGS.bouncy)
         e. 1200ms: XP counter text fades in below (motion.div opacity 0->1, y 20->0)
         f. 1800ms: Character quote fades in (motion.div opacity 0->1)
         g. 2500ms: call onComplete (auto-complete)
       - Use useSkippable(1000, onComplete) — tap to skip after 1s
       - onClick on overlay: call skip if available
       - exit animation: opacity 0, transition duration 0.3
       - Text styling: "100% COMPLETE" in text-4xl font-black text-warning, quote in text-lg text-text-secondary italic

    2. Create `frontend/src/components/animations/CapsuleDropOverlay.tsx`:
       - Props: `rewardTitle: string, rarity: string, onComplete: () => void`
       - State: `isRevealed: boolean` (false initially)
       - Phase 1 (capsule): Semi-transparent overlay, capsule div bounces in center with SPRINGS.bouncy (scale 0->1)
         - Capsule: 80x100px rounded-2xl div with gradient background, "?" text centered
         - Pulsing scale animation: `animate={{ scale: [1, 1.05, 1] }}` with `transition={{ duration: 1, repeat: Infinity }}`
         - onClick: set isRevealed=true
       - Phase 2 (reveal): 3D card flip — parent div with `perspective: 800`
         - motion.div with `animate={{ rotateY: isRevealed ? 180 : 0 }}` transition SPRINGS.snappy
         - Front face: capsule design (backfaceVisibility: 'hidden')
         - Back face: reward title + rarity glow (backfaceVisibility: 'hidden', transform: 'rotateY(180deg)')
         - Rarity glow colors: common -> white/gray shadow, rare -> blue shadow, epic -> purple shadow with pulsing boxShadow animation
       - Auto-dismiss: 4s after reveal OR tap after 1.5s minimum display
       - exit animation: scale 0.8, opacity 0

    3. Create `frontend/src/components/animations/DragonBallTrajectory.tsx`:
       - Props: `count: number, onComplete: () => void`
       - Render a golden ball (w-10 h-10 rounded-full bg-warning with star number) at center of viewport
       - Animate along arc path using keyframes:
         - x: [0, -30, -20, 0] (slight curve)
         - y: [0, -80, -150, -200] (moves upward toward tracker position)
         - scale: [1, 1.2, 1, 0.8] (grows slightly mid-arc, shrinks at end)
         - transition: duration 0.8, ease [0.25, 0.1, 0.25, 1]
       - On animation complete: call onComplete
       - exit: opacity 0, transition 0.2

    4. Update `frontend/src/components/dashboard/DragonBallTracker.tsx`:
       - Add `data-testid="dragon-ball-tracker"` to the container div for potential trajectory targeting
       - No other changes needed — the trajectory animation is overlay-based, not DOM-to-DOM

    5. Create `frontend/src/__tests__/perfect-day.test.tsx`:
       - Mock `motion/react`
       - Test: "renders overlay with 100% COMPLETE text"
       - Test: "calls onComplete callback"
       - Test: "renders particle burst container"

    6. Create `frontend/src/__tests__/capsule-drop.test.tsx`:
       - Mock `motion/react`
       - Test: "renders capsule with pulsing animation"
       - Test: "shows reward title and rarity after click"
       - Test: "applies correct glow color for each rarity" (test common/rare/epic)
       - Test: "calls onComplete callback"

    7. Create `frontend/src/__tests__/dragon-ball-trajectory.test.tsx`:
       - Mock `motion/react`
       - Test: "renders golden ball with star number"
       - Test: "calls onComplete after animation"
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - PerfectDayOverlay plays full choreographed sequence with shake, particles, text, quote
    - CapsuleDropOverlay bounces in, pulses, flips on tap, shows rarity glow
    - DragonBallTrajectory arcs from center upward
    - All three components accept onComplete and have exit animations
    - All tests pass including new overlay tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build TransformationOverlay, ShenronCeremony, and wire AnimationPlayer dispatcher</name>
  <files>
    frontend/src/components/animations/TransformationOverlay.tsx,
    frontend/src/components/animations/ShenronCeremony.tsx,
    frontend/src/components/animations/AnimationPlayer.tsx,
    frontend/src/components/dashboard/HeroSection.tsx,
    frontend/src/__tests__/transformation.test.tsx,
    frontend/src/__tests__/shenron-ceremony.test.tsx
  </files>
  <behavior>
    - TransformationOverlay shows form-specific flash (SSJ golden, SSJ2 lightning, SSG red, SSB blue, UI silver) with form name text, avatar swap, tap-to-skip after 1s, calls onComplete
    - ShenronCeremony plays multi-step sequence (darken, lightning, scale-up, wish text, scatter), enforces at least 1 active wish from rewardStore before allowing completion
    - AnimationPlayer switches on event.type to render correct overlay component, passing event data and onComplete
    - HeroSection renders TierChangeBanner when tier_change event is in animation queue
  </behavior>
  <action>
    1. Create `frontend/src/components/animations/TransformationOverlay.tsx`:
       - Props: `form: string, name: string, onComplete: () => void`
       - Form-to-visual mapping:
         - 'ssj': { color: 'from-yellow-500/80 to-yellow-300/40', flash: 'bg-yellow-300', label: 'Super Saiyan' }
         - 'ssj2': { color: 'from-yellow-500/80 to-blue-400/40', flash: 'bg-yellow-200', label: 'Super Saiyan 2' }
         - 'ssg': { color: 'from-red-600/80 to-red-400/40', flash: 'bg-red-400', label: 'Super Saiyan God' }
         - 'ssb': { color: 'from-blue-600/80 to-cyan-400/40', flash: 'bg-blue-400', label: 'Super Saiyan Blue' }
         - 'ui': { color: 'from-gray-400/80 to-white/40', flash: 'bg-gray-200', label: 'Ultra Instinct' }
         - Default fallback for unknown forms
       - Choreography:
         a. Mount: full-screen flash (motion.div from opacity 1 to 0 over 400ms)
         b. 300ms: gradient overlay with radial gradient (bg-gradient-radial) fades in
         c. 500ms: SaiyanAvatar component renders at center with new form, scales in with SPRINGS.bouncy
         d. 800ms: Form name text ("SUPER SAIYAN!") scales in below avatar, text-3xl font-black
         e. 2000ms: auto-complete via onComplete
       - useSkippable(1000, onComplete) for tap-to-skip
       - exit: opacity 0, scale 0.9

    2. Create `frontend/src/components/animations/ShenronCeremony.tsx`:
       - Props: `onComplete: () => void`
       - Import `useRewardStore` from store
       - Read `wishes` from rewardStore, filter to active wishes (`is_active === true`)
       - **ANIM-09 enforcement:** If no active wishes exist, show a banner "Set an active wish in Settings first!" with a dismiss button that calls onComplete. Do NOT proceed with the ceremony.
       - If active wishes exist, proceed with ceremony:
         a. Mount: sky darkens (full-screen overlay, bg-gradient from indigo-950/95 to black/95)
         b. 400ms: Lightning flashes — 2-3 rapid opacity pulses of white overlay (motion.div animate opacity [0, 1, 0, 1, 0] duration 0.6)
         c. 1000ms: "Shenron" text/silhouette scales up from bottom (motion.div y: 200->0, scale: 0.5->1, SPRINGS.gentle)
         d. 1500ms: "Your wish has been granted!" text fades in (motion.div opacity 0->1)
         e. 2500ms: ParticleBurst from center with golden particles
         f. 3500ms: "Balls scatter" — 7 small golden circles animate outward radially (each motion.div with randomized angle, distance 150-250px)
         g. 4500ms: everything fades out, call onComplete
       - useSkippable(1000, onComplete) for tap-to-skip
       - exit: opacity 0

    3. Update `frontend/src/components/animations/AnimationPlayer.tsx`:
       - Replace the placeholder overlay div with proper component dispatching
       - Import all five overlay components: PerfectDayOverlay, CapsuleDropOverlay, DragonBallTrajectory, TransformationOverlay, ShenronCeremony
       - Create a `renderOverlay(event: AnimationEvent, onComplete: () => void)` function that switches on event.type:
         - 'perfect_day': `<PerfectDayOverlay onComplete={onComplete} />`
         - 'capsule_drop': `<CapsuleDropOverlay rewardTitle={event.rewardTitle} rarity={event.rarity} onComplete={onComplete} />`
         - 'dragon_ball': `<DragonBallTrajectory count={event.count} onComplete={onComplete} />`
         - 'transformation': `<TransformationOverlay form={event.form} name={event.name} onComplete={onComplete} />`
         - 'shenron': `<ShenronCeremony onComplete={onComplete} />`
         - default: return null (safety — xp_popup and tier_change already filtered)
       - The `onComplete` callback should trigger the exit animation. The actual dequeue happens in `onExitComplete` of AnimatePresence.
       - **Important:** `onComplete` sets a local state `isDone` which removes the overlay content, triggering the AnimatePresence exit animation. `onExitComplete` then calls `dequeueAnimation()`.

    4. Update `frontend/src/components/dashboard/HeroSection.tsx`:
       - Import `TierChangeBanner` from '../animations/TierChangeBanner'
       - Import `useUiStore` from store
       - Subscribe to `animationQueue` to detect tier_change events
       - Find the latest tier_change event in the queue: `const tierEvent = animationQueue.find(e => e.type === 'tier_change')`
       - If tierEvent exists, render `<TierChangeBanner tier={tierEvent.tier} />` positioned absolutely above the AuraGauge
       - After TierChangeBanner auto-dismisses (1.5s), dequeue the tier_change event from uiStore
       - This keeps tier_change as inline (not through AnimationPlayer queue)

    5. Create `frontend/src/__tests__/transformation.test.tsx`:
       - Mock `motion/react`
       - Test: "renders golden flash for SSJ transformation"
       - Test: "renders blue flash for SSB transformation"
       - Test: "shows transformation name text"
       - Test: "renders SaiyanAvatar with correct form"
       - Test: "calls onComplete callback"

    6. Create `frontend/src/__tests__/shenron-ceremony.test.tsx`:
       - Mock `motion/react` and `../../store/rewardStore`
       - Test: "shows warning when no active wishes exist" (mock rewardStore.wishes as empty or all inactive)
       - Test: "renders ceremony when active wishes exist" (mock rewardStore.wishes with one active)
       - Test: "shows 'Your wish has been granted!' text during ceremony"
       - Test: "calls onComplete callback"
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - TransformationOverlay plays form-specific visual for all 5 forms (SSJ/SSJ2/SSG/SSB/UI)
    - ShenronCeremony plays full sequence when active wishes exist, blocks when none
    - AnimationPlayer dispatches to correct overlay based on event type
    - HeroSection renders inline TierChangeBanner from queue events
    - All full-screen overlays support tap-to-skip after 1s
    - All tests pass including new overlay and ceremony tests
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` — all tests pass
- AnimationPlayer correctly dispatches all 5 event types to overlay components
- Each overlay calls onComplete when its sequence finishes (or is skipped)
- Shenron ceremony blocks if no active wishes (ANIM-09)
- HeroSection shows inline TierChangeBanner for tier_change events
- All full-screen sequences support tap-to-skip after 1s
</verification>

<success_criteria>
- [ ] PerfectDayOverlay plays ~2.5s choreographed sequence with shake, particles, text, counter, quote
- [ ] CapsuleDropOverlay bounces in, pulses, flips on tap, shows rarity glow (white/blue/purple)
- [ ] DragonBallTrajectory arcs golden ball upward
- [ ] TransformationOverlay plays form-specific visual (golden/lightning/red/blue/silver) with avatar swap
- [ ] ShenronCeremony plays full sequence and enforces minimum 1 active wish (ANIM-09)
- [ ] AnimationPlayer dispatches to correct overlay per event type
- [ ] HeroSection renders inline TierChangeBanner
- [ ] All overlays support tap-to-skip after 1s
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-animation-layer/07-02-SUMMARY.md`
</output>
