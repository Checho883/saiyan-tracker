---
phase: 16-settings-forms-audio-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/habit/HabitForm.tsx
  - frontend/src/components/dashboard/HabitCard.tsx
autonomous: true
requirements: [HMGT-04, HMGT-05]

must_haves:
  truths:
    - "Frequency selector (Daily/Weekdays/Custom) is visible in the main form area, NOT hidden behind 'More options'"
    - "Custom day-of-week buttons are circular (w-9 h-9 rounded-full) with single-letter labels (M, T, W, T, F, S, S)"
    - "Selected day buttons use saiyan-500 fill, unselected use space-700 fill"
    - "Submit button is disabled when frequency is 'custom' and zero custom days are selected"
    - "HabitForm has a 'Temporary habit' toggle that reveals start_date and end_date native date pickers"
    - "Start date defaults to today, end date is required when temporary is toggled on"
    - "HabitForm passes is_temporary, start_date, and end_date to createHabit/updateHabit"
    - "HabitCard shows a subtle clock icon or 'temp' badge when habit.is_temporary is true"
  artifacts:
    - frontend/src/components/habit/HabitForm.tsx
    - frontend/src/components/dashboard/HabitCard.tsx
  key_links:
    - "HabitForm frequency state -> custom day picker visibility"
    - "HabitForm is_temporary toggle -> date picker visibility"
    - "HabitForm handleSubmit -> passes is_temporary/start_date/end_date to store"
    - "HabitCard habit.is_temporary -> renders clock badge"
---

<objective>
Refine HabitForm to expose temporary habit creation with date pickers, move frequency selector to the main form area with iOS-style circular day buttons, and add a temporary-habit indicator to HabitCard.

Purpose: Satisfies HMGT-04 (temporary habits with start/end dates) and HMGT-05 (tappable day-of-week buttons instead of raw input).
Output: Updated HabitForm.tsx with always-visible frequency, circular day picker, temporary toggle + date fields. Updated HabitCard.tsx with temp indicator.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-settings-forms-audio-polish/16-CONTEXT.md
@.planning/phases/16-settings-forms-audio-polish/16-RESEARCH.md
@frontend/src/components/habit/HabitForm.tsx
@frontend/src/components/dashboard/HabitCard.tsx
@frontend/src/types/index.ts

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/types/index.ts:
```typescript
export interface HabitCreate {
  title: string;
  attribute: Attribute;
  importance?: Importance;
  frequency?: Frequency;
  custom_days?: number[] | null;
  description?: string | null;
  icon_emoji?: string;
  category_id?: string | null;
  target_time?: string | null;
  is_temporary?: boolean;
  start_date: string; // YYYY-MM-DD
  end_date?: string | null;
  sort_order?: number;
}

export interface HabitUpdate {
  title?: string;
  attribute?: Attribute;
  importance?: Importance;
  frequency?: Frequency;
  custom_days?: number[] | null;
  description?: string | null;
  icon_emoji?: string;
  category_id?: string | null;
  target_time?: string | null;
  is_temporary?: boolean;
  start_date?: string;
  end_date?: string | null;
  sort_order?: number;
  is_active?: boolean;
}

export interface HabitTodayResponse {
  // ... includes:
  is_temporary: boolean;
  start_date: string;
  end_date: string | null;
}
```

From frontend/src/components/habit/HabitForm.tsx (current structure):
```typescript
// Frequency is inside {showMore && (...)} block — needs to move OUT
const FREQUENCY_OPTIONS: { value: Frequency; label: string }[] = [
  { value: 'daily', label: 'Daily' },
  { value: 'weekdays', label: 'Weekdays' },
  { value: 'custom', label: 'Custom' },
];
const DAY_LABELS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
// Custom days use: flex gap-1, flex-1 py-1.5 rounded text-xs
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move frequency out of "More options" and add circular day picker</name>
  <files>frontend/src/components/habit/HabitForm.tsx</files>
  <action>
1. **Change `DAY_LABELS` to single-letter labels:**
   Replace `const DAY_LABELS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];`
   with `const DAY_LABELS = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];`

2. **Move frequency section from inside `{showMore && (...)}` to the main form area.**
   Place the Frequency selector and Custom Days picker AFTER the Category `<div>` and BEFORE the "More options" toggle button. The frequency block currently lives inside the `{showMore && (<div className="space-y-4">` block — cut it out and paste it as a sibling of the Category section.

   The frequency section to move is:
   ```tsx
   {/* Frequency */}
   <div>
     <label className="block text-sm font-medium text-text-secondary mb-1">Frequency</label>
     <div className="flex gap-2">
       {FREQUENCY_OPTIONS.map((opt) => (...))}
     </div>
   </div>

   {/* Custom Days */}
   {frequency === 'custom' && (
     <div className="flex gap-1">
       {DAY_LABELS.map((label, i) => (...))}
     </div>
   )}
   ```

3. **Restyle custom day buttons to be circular iOS-style:**
   Replace the custom days `<div className="flex gap-1">` block with:
   ```tsx
   {frequency === 'custom' && (
     <div className="flex justify-center gap-2">
       {DAY_LABELS.map((label, i) => (
         <button
           key={i}
           type="button"
           onClick={() => toggleCustomDay(i)}
           className={`w-9 h-9 rounded-full text-sm font-semibold transition-colors ${
             customDays.includes(i)
               ? 'bg-saiyan-500 text-white'
               : 'bg-space-700 text-text-secondary'
           }`}
         >
           {label}
         </button>
       ))}
     </div>
   )}
   ```

4. **Update submit button disabled condition:**
   Change:
   ```tsx
   disabled={isSubmitting || !title.trim()}
   ```
   To:
   ```tsx
   disabled={isSubmitting || !title.trim() || (frequency === 'custom' && customDays.length === 0)}
   ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Frequency selector visible in main form (not behind "More options"). Day picker uses circular w-9 h-9 rounded-full buttons with single-letter labels. Submit disabled when custom frequency has zero days selected.</done>
</task>

<task type="auto">
  <name>Task 2: Add temporary habit toggle with date pickers to HabitForm</name>
  <files>frontend/src/components/habit/HabitForm.tsx</files>
  <action>
1. **Add state variables** (after existing state declarations):
   ```typescript
   const [isTemporary, setIsTemporary] = useState(habit?.is_temporary ?? false);
   const [startDate, setStartDate] = useState(habit?.start_date ?? new Date().toISOString().split('T')[0]);
   const [endDate, setEndDate] = useState(habit?.end_date ?? '');
   ```

2. **Add "Temporary habit" toggle section** AFTER the frequency/custom-days section and BEFORE the "More options" toggle button:
   ```tsx
   {/* Temporary Habit Toggle */}
   <div className="space-y-3">
     <div className="flex items-center justify-between">
       <label className="text-sm font-medium text-text-secondary">Temporary habit</label>
       <button
         type="button"
         role="switch"
         aria-checked={isTemporary}
         onClick={() => setIsTemporary(!isTemporary)}
         className={`relative w-11 h-6 rounded-full transition-colors ${
           isTemporary ? 'bg-saiyan-500' : 'bg-space-600'
         }`}
       >
         <div
           className={`absolute top-0.5 left-0.5 w-5 h-5 rounded-full bg-white transition-transform ${
             isTemporary ? 'translate-x-5' : 'translate-x-0'
           }`}
         />
       </button>
     </div>

     {isTemporary && (
       <div className="flex gap-3">
         <div className="flex-1">
           <label htmlFor="habit-start-date" className="block text-xs text-text-muted mb-1">
             Start date
           </label>
           <input
             id="habit-start-date"
             type="date"
             value={startDate}
             onChange={(e) => setStartDate(e.target.value)}
             className="w-full bg-space-700 border border-space-600 rounded-lg px-3 py-2 text-sm text-text-primary focus:outline-none focus:border-saiyan-500"
           />
         </div>
         <div className="flex-1">
           <label htmlFor="habit-end-date" className="block text-xs text-text-muted mb-1">
             End date
           </label>
           <input
             id="habit-end-date"
             type="date"
             value={endDate}
             onChange={(e) => setEndDate(e.target.value)}
             min={startDate}
             className="w-full bg-space-700 border border-space-600 rounded-lg px-3 py-2 text-sm text-text-primary focus:outline-none focus:border-saiyan-500"
           />
         </div>
       </div>
     )}
   </div>
   ```

3. **Update handleSubmit** to pass temporary fields:
   In the `createHabit` call, change `start_date: today` to:
   ```typescript
   is_temporary: isTemporary,
   start_date: isTemporary ? startDate : today,
   end_date: isTemporary ? (endDate || null) : null,
   ```

   In the `updateHabit` call, add:
   ```typescript
   is_temporary: isTemporary,
   start_date: isTemporary ? startDate : undefined,
   end_date: isTemporary ? (endDate || null) : null,
   ```

4. **Update submit disabled condition** to also require end_date when temporary:
   ```tsx
   disabled={isSubmitting || !title.trim() || (frequency === 'custom' && customDays.length === 0) || (isTemporary && !endDate)}
   ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Temporary habit toggle reveals start/end date native pickers. Start defaults to today. End date required when temporary is on. Form passes is_temporary, start_date, end_date to create/update actions. Submit disabled when temporary is on but no end date set.</done>
</task>

<task type="auto">
  <name>Task 3: Add temporary habit indicator to HabitCard</name>
  <files>frontend/src/components/dashboard/HabitCard.tsx</files>
  <action>
1. **Import Clock icon from lucide-react:**
   Add `Clock` to the existing import:
   ```typescript
   import { Check, MoreVertical, Pencil, Trash2, BarChart3, GripVertical, Clock } from 'lucide-react';
   ```

2. **Add temporary indicator in the bottom row** (after the attribute badge, before the streak):
   ```tsx
   {habit.is_temporary && (
     <span className="flex items-center gap-0.5 text-xs text-text-muted">
       <Clock className="w-3 h-3" />
       temp
     </span>
   )}
   ```

   Place this inside the `<div className="flex items-center gap-2 mt-2">` block, right after the attribute badge `<span>`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>HabitCard shows a small clock icon + "temp" text for temporary habits. Renders only when habit.is_temporary is true. Tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- Frequency selector (Daily/Weekdays/Custom) is in the main form, NOT inside "More options"
- Custom day buttons are circular `w-9 h-9 rounded-full` with single letters M/T/W/T/F/S/S
- "More options" toggle still works and contains only: Description, Icon, Target Time
- Temporary toggle reveals two date pickers when enabled
- Start date defaults to today, end date required (submit disabled without it)
- HabitCard shows clock + "temp" for temporary habits
- Submit button disabled when frequency=custom with 0 days selected
- Existing tests pass
</verification>

<success_criteria>
- [ ] Frequency is always visible in HabitForm (not behind "More options")
- [ ] Day picker uses circular iOS-style buttons with single-letter labels
- [ ] Submit disabled when custom frequency has zero days
- [ ] Temporary habit toggle reveals date pickers
- [ ] End date required when temporary is enabled
- [ ] HabitCard shows temporary indicator
- [ ] TypeScript compiles cleanly
- [ ] Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/16-settings-forms-audio-polish/16-01-SUMMARY.md`
</output>
