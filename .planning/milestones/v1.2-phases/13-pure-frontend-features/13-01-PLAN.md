---
phase: 13-pure-frontend-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/analytics/CapsuleHistoryList.tsx
  - frontend/src/components/analytics/WishHistoryList.tsx
  - frontend/src/pages/Analytics.tsx
  - frontend/src/__tests__/capsule-history.test.tsx
  - frontend/src/__tests__/wish-history.test.tsx
autonomous: true
requirements: [ANLT-01, ANLT-02]

must_haves:
  truths:
    - "User can view a scrollable list of past capsule drops with rarity, date, and reward name on the Analytics page"
    - "User can view a scrollable list of past Shenron wishes with date and wish description on the Analytics page"
  artifacts:
    - path: "frontend/src/components/analytics/CapsuleHistoryList.tsx"
      provides: "Capsule drop history list with rarity-colored pill badges"
      exports: ["CapsuleHistoryList"]
    - path: "frontend/src/components/analytics/WishHistoryList.tsx"
      provides: "Wish grant history list with dates"
      exports: ["WishHistoryList"]
  key_links:
    - from: "frontend/src/components/analytics/CapsuleHistoryList.tsx"
      to: "analyticsApi.capsuleHistory()"
      via: "useEffect fetch on mount"
      pattern: "analyticsApi\\.capsuleHistory"
    - from: "frontend/src/components/analytics/WishHistoryList.tsx"
      to: "analyticsApi.wishHistory()"
      via: "useEffect fetch on mount"
      pattern: "analyticsApi\\.wishHistory"
    - from: "frontend/src/pages/Analytics.tsx"
      to: "CapsuleHistoryList, WishHistoryList"
      via: "JSX import and render after PowerLevelChart"
      pattern: "<CapsuleHistoryList|<WishHistoryList"
---

<objective>
Add capsule drop history and wish grant history lists to the Analytics page.

Purpose: Users can explore their past capsule drops (with rarity, reward name, triggering habit, and date) and past Shenron wishes (with wish title and grant date) in scrollable lists below the existing charts.
Output: Two new analytics components rendered after PowerLevelChart on the Analytics page.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/pages/Analytics.tsx
@frontend/src/services/api.ts
@frontend/src/types/index.ts
@frontend/src/components/analytics/CalendarHeatmap.tsx

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/types/index.ts:
```typescript
export type Rarity = 'common' | 'rare' | 'epic';

export interface CapsuleHistoryItem {
  id: string;
  reward_title: string;
  reward_rarity: Rarity;
  habit_title: string;
  dropped_at: string;
}

export interface WishHistoryItem {
  id: string;
  wish_title: string;
  granted_at: string;
}
```

From frontend/src/services/api.ts:
```typescript
export const analyticsApi = {
  capsuleHistory: () => api.get('analytics/capsule-history').json<CapsuleHistoryItem[]>(),
  wishHistory: () => api.get('analytics/wish-history').json<WishHistoryItem[]>(),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CapsuleHistoryList and WishHistoryList components</name>
  <files>frontend/src/components/analytics/CapsuleHistoryList.tsx, frontend/src/components/analytics/WishHistoryList.tsx, frontend/src/__tests__/capsule-history.test.tsx, frontend/src/__tests__/wish-history.test.tsx</files>
  <behavior>
    - CapsuleHistoryList renders "Capsule History" heading
    - CapsuleHistoryList fetches data from analyticsApi.capsuleHistory() on mount with cancellation
    - Each capsule card shows: rarity pill badge (common=gray bg-gray-500, rare=blue bg-blue-500, epic=purple bg-purple-500), reward_title, habit_title, dropped_at date formatted
    - Shows first 10 items by default, "Show more" button reveals all when > 10 items
    - Shows "No capsule drops yet" empty state when list is empty
    - WishHistoryList renders "Wish History" heading
    - WishHistoryList fetches data from analyticsApi.wishHistory() on mount with cancellation
    - Each wish card shows: wish_title and granted_at date formatted
    - Shows first 10 items by default, "Show more" button reveals all when > 10 items
    - Shows "No wishes granted yet" empty state when list is empty
  </behavior>
  <action>
Create CapsuleHistoryList.tsx:
- useState for items (CapsuleHistoryItem[]), showAll (boolean)
- useEffect to fetch analyticsApi.capsuleHistory() with cancelled flag pattern (see useAnalyticsData.ts)
- Rarity color map: `const rarityColorMap: Record<Rarity, string> = { common: 'bg-gray-500 text-white', rare: 'bg-blue-500 text-white', epic: 'bg-purple-500 text-white' };`
- Container: `bg-space-800 rounded-xl p-4` (matches existing analytics card style)
- Each item rendered as a flex row card: rarity pill badge (text-xs font-bold uppercase px-2 py-0.5 rounded-full), reward title, habit title in text-text-muted, and formatted date on the right
- Slice to first 10 unless showAll, "Show more" button styled as `text-saiyan-500 text-sm font-medium`
- Format dates with toLocaleDateString()

Create WishHistoryList.tsx:
- Same pattern as CapsuleHistoryList but fetching analyticsApi.wishHistory()
- Each card shows wish_title and formatted granted_at date
- Same "Show more" / empty state pattern

Create test files:
- Mock analyticsApi using vi.mock('../../services/api')
- Test: renders heading, renders items with rarity badges, shows "Show more" when > 10, shows empty state
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/capsule-history.test.tsx src/__tests__/wish-history.test.tsx -x</automated>
  </verify>
  <done>Both history list components render with correct data, rarity badges, pagination, and empty states. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire history lists into Analytics page</name>
  <files>frontend/src/pages/Analytics.tsx</files>
  <action>
Import CapsuleHistoryList and WishHistoryList into Analytics.tsx.
Add both components AFTER the PowerLevelChart component in the JSX, in this order:
1. PowerLevelChart (existing)
2. CapsuleHistoryList (new)
3. WishHistoryList (new)

The Analytics page uses `space-y-6` so spacing is automatic.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Analytics page renders capsule and wish history lists after the power level chart. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- [ ] CapsuleHistoryList renders with rarity color-coded pill badges (gray/blue/purple)
- [ ] WishHistoryList renders with wish title and date
- [ ] Both lists show first 10 items with "Show more" button
- [ ] Both lists show empty state when no data
- [ ] Lists appear below PowerLevelChart on Analytics page
- [ ] All tests pass: `cd frontend && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- CapsuleHistoryList component renders capsule drops with rarity badges, reward names, habit titles, dates
- WishHistoryList component renders wish grants with titles and dates
- Both support "Show more" pagination (10 initial, expand to show all)
- Both display empty states
- Both integrated into Analytics page after PowerLevelChart
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-pure-frontend-features/13-01-SUMMARY.md`
</output>
