---
phase: 24-vps-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/database/session.py, backend/tests/test_wal_mode.py]
autonomous: true
requirements: [DEPLOY-11]

must_haves:
  truths:
    - "SQLite WAL pragma is executed on every new database connection"
    - "WAL mode test passes confirming pragma is set"
  artifacts:
    - backend/app/database/session.py
    - backend/tests/test_wal_mode.py
  key_links:
    - "set_sqlite_pragma event handler sets both foreign_keys and journal_mode"
---

<objective>
Enable SQLite WAL (Write-Ahead Logging) mode in the backend database connection setup.

Purpose: WAL mode prevents database locking issues when the service handles concurrent reads and writes, and ensures the database file persists cleanly across service restarts without phantom journals.
Output: Modified session.py with WAL pragma, test confirming WAL mode is set.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/database/session.py
</context>

<interfaces>
<!-- Existing code the executor needs to modify -->

From backend/app/database/session.py:
```python
@event.listens_for(engine, "connect")
def set_sqlite_pragma(dbapi_connection, connection_record):
    cursor = dbapi_connection.cursor()
    cursor.execute("PRAGMA foreign_keys=ON")
    cursor.close()
```

From backend/app/core/config.py:
```python
class Settings(BaseSettings):
    DATABASE_URL: str = "sqlite:///saiyan_tracker.db"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WAL pragma to SQLite connection handler</name>
  <files>backend/app/database/session.py, backend/tests/test_wal_mode.py</files>
  <behavior>
    - Test 1: When a new database connection is established, PRAGMA journal_mode returns "wal"
    - Test 2: PRAGMA foreign_keys still returns ON (regression check)
  </behavior>
  <action>
1. In `backend/app/database/session.py`, add `cursor.execute("PRAGMA journal_mode=WAL")` inside the existing `set_sqlite_pragma` event handler, right after the `PRAGMA foreign_keys=ON` line and before `cursor.close()`.

2. Create `backend/tests/test_wal_mode.py` with tests that:
   - Create a fresh SQLAlchemy engine with an in-memory or temp SQLite database
   - Register the same pragma event handler
   - Open a connection and verify `PRAGMA journal_mode` returns `wal` (note: in-memory databases may return `memory` — use a temp file-based database)
   - Verify `PRAGMA foreign_keys` returns `1`

Use a temporary file database (`sqlite:///` + tmpdir path) to test WAL mode since in-memory databases don't support WAL. Use pytest's `tmp_path` fixture.

Do NOT modify the function signature or the event listener registration. Only add the one new pragma line.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_wal_mode.py -v</automated>
  </verify>
  <done>
    - `PRAGMA journal_mode=WAL` is set in `set_sqlite_pragma` handler
    - `PRAGMA foreign_keys=ON` still present (not removed or broken)
    - Test file exists and passes confirming both pragmas work
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_wal_mode.py -v` passes
- `session.py` contains both `PRAGMA foreign_keys=ON` and `PRAGMA journal_mode=WAL`
</verification>

<success_criteria>
- WAL pragma added to the existing connect event handler
- Test confirms WAL mode is active on file-based SQLite databases
- Existing foreign_keys pragma unchanged
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-vps-infrastructure/24-01-SUMMARY.md`
</output>
