---
phase: 11-animation-queue-refactor-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: [TECH-01]

must_haves:
  truths:
    - The recharts charts render without a react-is override hack in package.json
    - PowerLevelChart and AttributeChart render correctly with no visual changes
    - No peer dependency warnings related to react-is appear during npm install
  artifacts:
    - frontend/package.json (overrides block removed)
    - frontend/package-lock.json (regenerated without override)
  key_links:
    - recharts@3.7.0 -> react-is@19.2.4 (native resolution, no override needed)
    - PowerLevelChart.tsx -> recharts LineChart
    - AttributeChart.tsx -> recharts AreaChart
---

<objective>
Remove the vestigial react-is override hack from package.json.

Purpose: recharts 3.7.0 natively resolves react-is@19.2.4 (compatible with React 19). The `"overrides": { "react-is": "^19.0.0" }` block in package.json is no longer needed and constitutes tech debt.

Output: Clean package.json without overrides, verified chart rendering.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-animation-queue-refactor-tech-debt/11-CONTEXT.md

<interfaces>
<!-- Key files affected by this change -->

From frontend/package.json (current overrides block to remove):
```json
"overrides": {
  "react-is": "^19.0.0"
},
```

Recharts consumers (no changes needed, just verify):
- frontend/src/components/analytics/PowerLevelChart.tsx (uses LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer)
- frontend/src/components/analytics/AttributeChart.tsx (uses AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer)

Existing test coverage:
- frontend/src/__tests__/analytics-charts.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove react-is override and verify charts</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
1. Edit `frontend/package.json`:
   - Remove the entire `"overrides"` block:
     ```json
     "overrides": {
       "react-is": "^19.0.0"
     },
     ```
   - Ensure no trailing comma issues after removal

2. Regenerate lock file:
   ```bash
   cd frontend && npm install
   ```

3. Verify react-is resolves correctly:
   ```bash
   cd frontend && npm ls react-is
   ```
   Expected: react-is@19.x.x resolved through recharts and @testing-library/react without overrides

4. Run chart-related tests to verify no regressions:
   ```bash
   cd frontend && npx vitest run src/__tests__/analytics-charts.test.tsx --reporter=verbose
   ```

5. If `npm install` produces peer dependency warnings about react-is:
   - Add `react-is` as an explicit devDependency: `npm install -D react-is@^19.0.0`
   - This is cleaner than the overrides hack

6. Run full test suite to confirm no regressions:
   ```bash
   cd frontend && npx vitest run --reporter=verbose
   ```
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/analytics-charts.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - package.json has no "overrides" block
    - `npm ls react-is` shows clean resolution (no UNMET PEER DEP warnings)
    - analytics-charts tests pass
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
1. Confirm `"overrides"` does not appear in package.json: `grep -c "overrides" frontend/package.json` returns 0
2. Verify react-is resolution: `cd frontend && npm ls react-is` shows no errors
3. Run chart tests: `cd frontend && npx vitest run src/__tests__/analytics-charts.test.tsx`
4. Run full suite: `cd frontend && npx vitest run`
</verification>

<success_criteria>
- package.json has no overrides block
- react-is resolves cleanly through normal dependency resolution
- PowerLevelChart and AttributeChart render correctly (existing tests pass)
- No peer dependency warnings during npm install
</success_criteria>

<output>
After completion, create `.planning/phases/11-animation-queue-refactor-tech-debt/11-02-SUMMARY.md`
</output>
