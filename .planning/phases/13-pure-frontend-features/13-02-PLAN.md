---
phase: 13-pure-frontend-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dashboard/HabitDetailSheet.tsx
  - frontend/src/components/dashboard/ContributionGrid.tsx
  - frontend/src/components/dashboard/HabitCard.tsx
  - frontend/src/__tests__/contribution-graph.test.tsx
autonomous: true
requirements: [ANLT-03]

must_haves:
  truths:
    - "User can view a GitHub-style 90-day contribution grid for any individual habit showing daily completion"
    - "Tapping a habit card opens a detail sheet with contribution grid, current streak, best streak, and total completions"
  artifacts:
    - path: "frontend/src/components/dashboard/ContributionGrid.tsx"
      provides: "GitHub-style 90-day contribution grid"
      exports: ["ContributionGrid"]
    - path: "frontend/src/components/dashboard/HabitDetailSheet.tsx"
      provides: "Bottom sheet with contribution grid and streak stats"
      exports: ["HabitDetailSheet"]
  key_links:
    - from: "frontend/src/components/dashboard/HabitDetailSheet.tsx"
      to: "habitsApi.contributionGraph(id, 90)"
      via: "useEffect fetch on open"
      pattern: "habitsApi\\.contributionGraph"
    - from: "frontend/src/components/dashboard/HabitCard.tsx"
      to: "HabitDetailSheet"
      via: "onClick opens detail sheet via uiStore modal"
      pattern: "habit-detail-"
---

<objective>
Add per-habit detail sheet with GitHub-style contribution grid, accessible by tapping a habit card on the Dashboard.

Purpose: Users can view a 90-day contribution grid for any habit, showing daily completion history, current streak, best streak, and total completions -- without navigating away from the Dashboard.
Output: A new detail sheet component with contribution grid, wired to habit card tap on the Dashboard.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/dashboard/HabitCard.tsx
@frontend/src/store/uiStore.ts
@frontend/src/services/api.ts
@frontend/src/types/index.ts
@frontend/src/components/analytics/CalendarHeatmap.tsx

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/types/index.ts:
```typescript
export interface ContributionDay {
  date: string;
  completed: boolean;
}

export interface HabitTodayResponse extends HabitResponse {
  completed: boolean;
  streak_current: number;
  streak_best: number;
}
```

From frontend/src/services/api.ts:
```typescript
export const habitsApi = {
  contributionGraph: (id: string, days?: number) =>
    api.get(`habits/${id}/contribution-graph`, { searchParams: days ? { days } : {} }).json<ContributionDay[]>(),
};
```

From frontend/src/store/uiStore.ts:
```typescript
// Modal state pattern
activeModal: string | null;
openModal: (id: string) => void;
closeModal: () => void;
```

From frontend/src/components/dashboard/HabitCard.tsx:
```typescript
// Existing menu uses openModal pattern:
openModal(`habit-edit-${habit.id}`);
openModal(`habit-delete-${habit.id}`);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ContributionGrid component</name>
  <files>frontend/src/components/dashboard/ContributionGrid.tsx, frontend/src/__tests__/contribution-graph.test.tsx</files>
  <behavior>
    - ContributionGrid accepts days: ContributionDay[] prop
    - Renders a GitHub-style grid: 7 rows (days of week, Sun-Sat) x ~13 columns (weeks)
    - Most recent week on the right side
    - Completed days show as green cells (4 shades not needed -- binary completed/not, use green vs space-700)
    - Non-completed days show as bg-space-700 cells
    - Null/padding cells for alignment are transparent
    - Day-of-week labels on left (S, M, T, W, T, F, S)
    - Renders correct number of cells for 90 days of data
    - Handles empty data gracefully
  </behavior>
  <action>
Create ContributionGrid.tsx:
- Props: `{ days: ContributionDay[] }`
- Build grid structure: organize 90 days into weeks (columns). Each column = 7 cells (Sun through Sat).
- Start from 90 days ago, end at today. Pad the first week so the first day lands on its correct weekday.
- Cell styling: `w-3 h-3 rounded-sm` -- completed = `bg-green-500`, not completed = `bg-space-700`
- Container uses CSS grid: `grid-template-rows: repeat(7, 1fr)` with `grid-auto-flow: column`
- Day labels on the left column: S, M, T, W, T, F, S in text-text-muted text-xs
- Gap: `gap-1`

Color scheme (Claude's discretion -- using green like GitHub for universal recognition):
- Completed: `bg-green-500`
- Not completed: `bg-space-700`

Create test file contribution-graph.test.tsx:
- Mock ContributionDay data for 90 days
- Test: renders correct number of cells
- Test: completed days get green styling
- Test: empty data renders without error
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/contribution-graph.test.tsx -x</automated>
  </verify>
  <done>ContributionGrid renders a 90-day GitHub-style grid with green cells for completed days and dark cells for missed days. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create HabitDetailSheet and wire to HabitCard</name>
  <files>frontend/src/components/dashboard/HabitDetailSheet.tsx, frontend/src/components/dashboard/HabitCard.tsx</files>
  <action>
Create HabitDetailSheet.tsx:
- Props: `{ habitId: string; habitTitle: string; habitEmoji: string; streakCurrent: number; streakBest: number; onClose: () => void }`
- Uses motion/react for slide-up animation (bottom sheet pattern):
  - Backdrop: fixed inset-0 bg-black/60 z-50, onClick calls onClose
  - Sheet: fixed bottom-0 left-0 right-0, bg-space-800 rounded-t-2xl p-5, max-h-[70vh] overflow-y-auto
  - Initial: { y: '100%' }, animate: { y: 0 }, exit: { y: '100%' }, transition: spring with stiffness 300, damping 30
- Content layout:
  - Header: habit emoji + title, close button (X icon from lucide-react)
  - Stats row: Current Streak, Best Streak, Total Completions (count completed=true from data) in flex row
  - ContributionGrid component with fetched data
  - Legend: "Completed" with green square, "Missed" with dark square
- Fetch data: useEffect with habitsApi.contributionGraph(habitId, 90), cancelled flag pattern
- No attribute XP in this view (per user decision)
- Total completions = count of days where completed === true from API response

Modify HabitCard.tsx:
- Import HabitDetailSheet and AnimatePresence from motion/react
- Add state: `const [showDetail, setShowDetail] = useState(false);`
- The card body (the main card div, not the menu) gets a long-press or simple tap behavior. Currently the entire card tap calls handleTap (checkHabit). We need to differentiate:
  - Tap the card body area → still calls checkHabit (existing behavior)
  - Add a new small button (e.g., a bar chart icon from lucide-react) in the top row near the menu button, that opens the detail sheet
  - This avoids conflicting with the existing tap-to-check behavior
- When detail sheet is open, render HabitDetailSheet inside AnimatePresence
- The detail sheet button should use BarChart3 icon from lucide-react, styled like the MoreVertical button
- Pass habit.id, habit.title, habit.icon_emoji, habit.streak_current, habit.streak_best, and onClose={() => setShowDetail(false)}
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>HabitCard has a chart icon button that opens HabitDetailSheet. Sheet slides up from bottom with contribution grid, streak stats, and total completions. All tests pass.</done>
</task>

</tasks>

<verification>
- [ ] Tapping chart icon on a habit card opens a bottom sheet
- [ ] Bottom sheet shows 90-day contribution grid in GitHub style
- [ ] Green cells for completed days, dark cells for missed days
- [ ] Stats show current streak, best streak, total completions
- [ ] No attribute XP shown in the detail sheet
- [ ] Sheet closes on backdrop tap or close button
- [ ] All tests pass: `cd frontend && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- ContributionGrid renders 90-day grid with correct weekday alignment
- HabitDetailSheet slides up from bottom with animation
- Sheet displays contribution grid and streak statistics
- HabitCard has a chart icon button to open detail sheet without conflicting with tap-to-check
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-pure-frontend-features/13-02-SUMMARY.md`
</output>
