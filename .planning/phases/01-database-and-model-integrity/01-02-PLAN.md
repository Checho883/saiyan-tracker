---
phase: 01-database-and-model-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/app/database/seed.py
  - backend/app/main.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_models.py
  - backend/tests/test_constraints.py
  - backend/tests/test_seed.py
  - backend/tests/test_constants.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
  - DB-06
  - DB-07
  - DB-08
must_haves:
  truths:
    - "100+ quotes are seeded with correct character distribution, trigger events, and severity levels"
    - "Default user, 6 categories, ~10 rewards, and 3-5 wishes are seeded on DB init"
    - "Seed functions are idempotent — running twice does not duplicate data"
    - "UniqueConstraints reject duplicate (habit_id, log_date) and (user_id, log_date) entries"
    - "FK violations raise IntegrityError when PRAGMA foreign_keys=ON"
    - "Category deletion sets habit category_id to NULL"
    - "All tests pass: models, constraints, seed data, constants"
  artifacts:
    - path: "backend/app/database/seed.py"
      provides: "All seed data functions"
      exports: ["seed_all"]
    - path: "backend/tests/conftest.py"
      provides: "Test fixtures with in-memory SQLite"
      contains: "sqlite:///:memory:"
    - path: "backend/tests/test_models.py"
      provides: "Model field and table creation tests"
      min_lines: 50
    - path: "backend/tests/test_constraints.py"
      provides: "Constraint enforcement tests"
      min_lines: 30
    - path: "backend/tests/test_seed.py"
      provides: "Seed data and idempotency tests"
      min_lines: 30
    - path: "backend/tests/test_constants.py"
      provides: "Constants importability and value tests"
      min_lines: 20
  key_links:
    - from: "backend/app/database/seed.py"
      to: "backend/app/models/*.py"
      via: "Model imports for seed data creation"
      pattern: "from app.models"
    - from: "backend/app/main.py"
      to: "backend/app/database/seed.py"
      via: "seed_all called on startup"
      pattern: "seed_all"
    - from: "backend/tests/conftest.py"
      to: "backend/app/database/base.py"
      via: "In-memory SQLite engine for tests"
      pattern: "create_engine.*memory"
---

<objective>
Create all seed data (default user, categories, rewards, wishes, 100+ quotes) and a comprehensive test suite validating every model, constraint, seed function, and constant from Phase 1.

Purpose: Seed data makes the app usable from first run. Tests prove the data contract is correct before Phase 2 builds game logic on top of it.
Output: seed.py with idempotent seed functions, 4 test files covering all DB requirements, pytest configuration.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-and-model-integrity/01-RESEARCH.md
@.planning/phases/01-database-and-model-integrity/01-CONTEXT.md
@.planning/phases/01-database-and-model-integrity/01-01-SUMMARY.md
@PRD.md (Section 5: Character Quote System, Section 8: Database Schema)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed data with 100+ quotes</name>
  <files>
    backend/app/database/seed.py,
    backend/app/main.py
  </files>
  <action>
    Create `backend/app/database/seed.py` with idempotent seed functions. All seed functions must check for existing data before inserting (query first, insert only if absent).

    **seed_default_user(db):**
    - Check if user with username="default-user" exists
    - If not, create User(username="default-user", display_name="Saiyan", sound_enabled=True, theme="dark")
    - Return the user (existing or new) — other seed functions need user_id

    **seed_default_categories(db, user_id):**
    - Check if categories exist for this user
    - If count == 0, insert 6 categories:
      - Health (color: #FF4444, icon: ❤️), Mind (color: #4488FF, icon: 🧠), Body (color: #44CC44, icon: 💪), Family (color: #FF8844, icon: 👨‍👩‍👧‍👦), Skills (color: #AA44FF, icon: 🎯), Discipline (color: #FFCC00, icon: 🔥)
    - Set sort_order = 0, 1, 2, 3, 4, 5

    **seed_default_rewards(db, user_id):**
    - Check if rewards exist for this user
    - If count == 0, insert ~10 rewards across rarities:
      - Common: "15 min TikTok", "Eat a snack", "10 min break", "Listen to a song", "5 min social media", "Stretch break"
      - Rare: "30 min gaming", "Buy a coffee", "Watch an episode"
      - Epic: "Order takeout"

    **seed_default_wishes(db, user_id):**
    - Check if wishes exist for this user
    - If count == 0, insert 4 wishes:
      - "Buy a new game", "Full day off", "Nice dinner out", "New gear purchase"

    **seed_quotes(db):**
    - Check if quotes exist (count > 0 means skip)
    - If count == 0, bulk insert 100+ quotes
    - Character distribution: ~30% Goku, ~25% Vegeta, ~15% Piccolo, ~10% each Gohan/Whis/Beerus
    - Trigger event coverage: habit_complete, perfect_day, streak_milestone, transformation, zenkai, roast, welcome_back
    - Quote content guidelines:
      - Use REAL iconic Dragon Ball Z/Super quotes where they naturally fit:
        - Goku transformation: "I am the Super Saiyan, Son Goku!" (Namek Saga)
        - Goku perfect_day: "I've never been this excited before!" (various)
        - Vegeta transformation: "I am the prince of all Saiyans!" (Namek Saga)
        - Gohan transformation: "Fight you? No. I wanna kill you." (Cell Saga)
      - Original in-character quotes for app-specific triggers:
        - habit_complete: encouragement from Goku, acknowledgment from Piccolo
        - zenkai: comeback quotes emphasizing Saiyan biology
        - welcome_back: returning after absence
      - Vegeta roast quotes (trigger_event="roast"):
        - severity="mild": Dismissive, condescending ("Hmph. Even Kakarot wouldn't skip a day.")
        - severity="medium": Disappointment, challenge ("You're falling behind, and I won't wait for you.")
        - severity="savage": Pride-destroying, vicious ("You call yourself a Saiyan? You're nothing but a disgrace to our race. Even Raditz had more dedication.")
        - At least 5 roast quotes per severity level
      - Transformation quotes: One per transformation per relevant character. Set transformation_level field.
      - Streak milestone quotes: Set trigger_event="streak_milestone"
    - Source sagas: Namek Saga, Cell Saga, Buu Saga, DBS Universe 6, DBS Goku Black, DBS Tournament of Power, DBS Broly, DBS Super Hero, Original

    **seed_all(db):**
    - Call all seed functions in order: user -> categories -> rewards -> wishes -> quotes
    - Pass user_id from seed_default_user to other functions

    **Update backend/app/main.py:**
    - Uncomment/add the seed import and call
    - In the startup/lifespan event: after create_all, open a session, call seed_all(db), close session

    **IMPORTANT:** Vegeta savage roasts must feel like actual Vegeta — pride, superiority, disappointment in Saiyan heritage. Not generic insults. Per user decision: "pride-destroying, the kind that makes you open the app out of spite."
  </action>
  <verify>
    <automated>cd backend && python -c "
from app.database.base import Base
from app.database.session import engine, SessionLocal
import app.models
from app.database.seed import seed_all
from app.models.quote import Quote
Base.metadata.create_all(engine)
db = SessionLocal()
seed_all(db)
count = db.query(Quote).count()
print(f'Quotes seeded: {count}')
assert count >= 100, f'Expected 100+ quotes, got {count}'
# Check character distribution
from sqlalchemy import func
chars = db.query(Quote.character, func.count()).group_by(Quote.character).all()
for c, n in sorted(chars): print(f'  {c}: {n}')
# Check roast severities
roasts = db.query(Quote).filter(Quote.trigger_event == 'roast').count()
print(f'Roast quotes: {roasts}')
assert roasts >= 15, f'Expected 15+ roasts, got {roasts}'
db.close()
print('OK')
" && rm -f saiyan_tracker.db</automated>
  </verify>
  <done>
    - seed.py contains idempotent seed functions for user, categories, rewards, wishes, quotes
    - 100+ quotes seeded with correct character distribution (~30% Goku, ~25% Vegeta, ~15% Piccolo, ~10% each others)
    - All 7 trigger events covered
    - At least 15 roast quotes (5 per severity: mild, medium, savage)
    - Vegeta savage roasts are pride-destroying and in-character
    - Real iconic quotes used for transformations and perfect days
    - main.py calls seed_all on startup after create_all
    - Running seed_all twice does not duplicate data
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create comprehensive test suite</name>
  <files>
    backend/pyproject.toml,
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/tests/test_models.py,
    backend/tests/test_constraints.py,
    backend/tests/test_seed.py,
    backend/tests/test_constants.py
  </files>
  <behavior>
    - test_create_all_tables: All 15 tables created from SQLAlchemy models (DB-01)
    - test_user_defaults: User model has str_xp, vit_xp, int_xp, ki_xp, power_level, current_transformation, dragon_balls_collected, wishes_granted, sound_enabled, theme with correct defaults (DB-02)
    - test_habit_fields: Habit has importance and attribute fields, no base_points column (DB-03)
    - test_habit_log_fields: HabitLog has attribute_xp_awarded and capsule_dropped (DB-04)
    - test_daily_log_fields: DailyLog has is_perfect_day, completion_tier, xp_earned, streak_multiplier, zenkai_bonus_applied, dragon_ball_earned (DB-05)
    - test_date_columns_are_strings: log_date, off_date, start_date, end_date stored as strings not datetime (DB-06)
    - test_category_fields: Category has name, color_code, icon; no point_multiplier (DB-08)
    - test_unique_habit_log: Duplicate (habit_id, log_date) raises IntegrityError (DB-04)
    - test_unique_daily_log: Duplicate (user_id, log_date) raises IntegrityError (DB-05)
    - test_fk_enforcement: Invalid FK raises IntegrityError (proves PRAGMA works)
    - test_category_set_null: Deleting category sets habit.category_id to NULL (DB-08)
    - test_seed_quotes: seed_quotes creates 100+ quotes with all required fields (DB-07)
    - test_seed_idempotent: Running seed_all twice produces same record counts
    - test_seed_character_distribution: Quote character counts match expected distribution
    - test_seed_default_data: Default user, categories, rewards, wishes created
    - test_constants_importable: All constants import and have correct types/values
    - test_transformations_count: 10 transformations defined
    - test_importance_xp_values: normal=15, important=22, critical=30
  </behavior>
  <action>
    **backend/pyproject.toml:**
    ```toml
    [tool.pytest.ini_options]
    testpaths = ["tests"]
    pythonpath = ["."]
    ```

    **backend/tests/__init__.py:** Empty file.

    **backend/tests/conftest.py:**
    Create shared fixtures:
    - `engine` fixture (session scope): create_engine("sqlite:///:memory:"), add FK pragma event listener, create all tables
    - `db` fixture (function scope): create session, yield, rollback, close
    - Import all models in conftest so create_all discovers them
    - Create helper fixture `sample_user` that creates and returns a test User

    **backend/tests/test_models.py (DB-01 through DB-06, DB-08):**
    - test_create_all_tables: Assert 15 tables exist in Base.metadata.tables
    - test_user_defaults: Create User(), assert default values for all fields
    - test_habit_fields: Create Habit with importance="critical", attribute="str", verify fields; assert "base_points" not in column names
    - test_habit_log_fields: Create HabitLog, verify attribute_xp_awarded and capsule_dropped fields
    - test_daily_log_fields: Create DailyLog, verify all 6 required fields exist with defaults
    - test_date_columns_are_strings: Insert HabitLog with log_date="2026-03-04", read it back, assert it's a string (not datetime)
    - test_category_fields: Create Category, verify name/color_code/icon exist; assert "point_multiplier" not in column names
    - test_all_model_tables: Verify each of the 15 table names exists

    **backend/tests/test_constraints.py:**
    - test_unique_habit_log: Insert two HabitLogs with same (habit_id, log_date), assert IntegrityError on commit
    - test_unique_daily_log: Insert two DailyLogs with same (user_id, log_date), assert IntegrityError on commit
    - test_fk_enforcement: Insert HabitLog with non-existent habit_id, assert IntegrityError (proves PRAGMA foreign_keys=ON works)
    - test_category_set_null: Create category, create habit with that category_id, delete category, verify habit.category_id is NULL after refresh
    - test_wish_log_cascade: Create wish, create wish_log, delete wish, verify wish_log also deleted

    **backend/tests/test_seed.py (DB-07):**
    - test_seed_quotes: Call seed_quotes, assert count >= 100
    - test_seed_quotes_fields: Verify all quotes have character, quote_text, source_saga, trigger_event
    - test_seed_character_distribution: Goku >= 25, Vegeta >= 20, Piccolo >= 10
    - test_seed_trigger_coverage: All 7 trigger events have at least 1 quote
    - test_seed_roast_severities: At least 5 quotes per severity (mild, medium, savage)
    - test_seed_default_user: seed_default_user creates user with correct defaults
    - test_seed_default_categories: 6 categories with correct names
    - test_seed_default_rewards: ~10 rewards across 3 rarities
    - test_seed_default_wishes: 3-5 wishes
    - test_seed_idempotent: Call seed_all twice, assert counts unchanged

    **backend/tests/test_constants.py:**
    - test_transformations_count: len(TRANSFORMATIONS) == 10
    - test_transformation_thresholds: First is 0, last is 150000
    - test_importance_xp_values: normal=15, important=22, critical=30
    - test_completion_tiers: 4 tiers, highest multiplier is 2.0
    - test_streak_constants: STREAK_BONUS_PER_DAY=0.05, cap=1.0
    - test_capsule_constants: drop_chance=0.25, rarity weights sum to 1.0
    - test_valid_enums: All VALID_* lists are non-empty
    - test_dragon_balls_required: == 7
    - test_attribute_titles: All 4 attributes have title entries
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All tests pass: `python -m pytest tests/ -v` shows green
    - test_models.py covers DB-01, DB-02, DB-03, DB-04, DB-05, DB-06, DB-08
    - test_constraints.py covers unique constraints, FK enforcement, SET NULL cascade, CASCADE delete
    - test_seed.py covers DB-07 (100+ quotes, character distribution, trigger coverage, roast severities) and seed idempotency
    - test_constants.py covers all game constant values
    - conftest.py provides in-memory SQLite with FK pragma for fast, isolated tests
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd backend && python -m pytest tests/ -v` — ALL tests pass
2. `cd backend && python -m pytest tests/test_seed.py::test_seed_quotes -v` — 100+ quotes verified
3. `cd backend && python -m pytest tests/test_constraints.py -v` — all constraints enforced
4. `cd backend && python -c "from app.database.seed import seed_all"` — seed importable
</verification>

<success_criteria>
- 100+ quotes seeded with correct character distribution and all 7 trigger events
- Default user, 6 categories, ~10 rewards, 4 wishes seeded
- Seed functions are idempotent (safe to run twice)
- All tests pass covering every DB requirement (DB-01 through DB-08)
- Tests use in-memory SQLite for speed and isolation
- FK pragma is verified by test_fk_enforcement
- Unique constraints verified by test_unique_habit_log and test_unique_daily_log
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-and-model-integrity/01-02-SUMMARY.md`
</output>
