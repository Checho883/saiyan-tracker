---
phase: 18-dashboard-polish-responsive-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Analytics.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/components/dashboard/StatsPanel.tsx
autonomous: true
requirements: [RESP-03]

must_haves:
  truths:
    - Dashboard content is centered and does not stretch beyond 768px on a 1440px viewport
    - All three pages (Dashboard, Analytics, Settings) have consistent px-4 padding and uniform spacing
    - StatsPanel, HabitList, and hero section have uniform vertical gaps (space-y-4 or equivalent)
  artifacts:
    - frontend/src/components/layout/AppShell.tsx with max-w-3xl container
  key_links:
    - AppShell container wraps Outlet content
    - All pages benefit from centered container without individual page changes
---

<objective>
Add a global max-width content container and polish desktop spacing for consistent alignment.

Purpose: Ensure the app looks clean and professional on desktop (1440px) with centered content and uniform gaps before adding mobile breakpoints.
Output: AppShell with max-w-3xl centered container, pages with consistent spacing.
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dashboard-polish-responsive-design/18-RESEARCH.md

<interfaces>
<!-- AppShell current structure -->
From frontend/src/components/layout/AppShell.tsx:
- AppShell renders: SoundProvider > MotionConfig > div.min-h-screen.bg-space-900.pb-16 > [Outlet, AnimationPlayer, BottomTabBar]
- BottomTabBar and AnimationPlayer are fixed/absolute positioned, so they stay OUTSIDE the content container
- Outlet is the only element that needs the max-width wrapper

From frontend/src/pages/Dashboard.tsx:
- Root: div.pb-20 containing MiniHero (fixed), sentinelRef, RoastWelcomeCard, HeroSection, StatsPanel (px-4 mt-4), HabitList (px-4 mt-4), FAB (fixed), sheets
- FAB is fixed positioned (bottom-20 right-4) — stays outside container

From frontend/src/pages/Analytics.tsx:
- Root: div.p-4.space-y-6.pb-24 — already has consistent spacing

From frontend/src/pages/Settings.tsx:
- Root: div.p-4.space-y-4.pb-24 — already has consistent spacing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add max-width content container to AppShell</name>
  <files>frontend/src/components/layout/AppShell.tsx</files>
  <action>
In AppShell.tsx, wrap the `<Outlet />` in a centered max-width container div. The container should NOT wrap AnimationPlayer or BottomTabBar (they are fixed/absolute positioned and need full viewport width).

Change the main content div from:
```tsx
<div className="min-h-screen bg-space-900 pb-16">
  <Outlet />
  <AnimationPlayer />
  <BottomTabBar />
</div>
```

To:
```tsx
<div className="min-h-screen bg-space-900 pb-16">
  <div className="mx-auto max-w-3xl">
    <Outlet />
  </div>
  <AnimationPlayer />
  <BottomTabBar />
</div>
```

This centers all page content at max 768px width on large screens while allowing fixed elements (BottomTabBar, AnimationPlayer, FAB, overlays) to remain full-viewport.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>AppShell wraps Outlet in mx-auto max-w-3xl container. AnimationPlayer and BottomTabBar remain outside the container. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize Dashboard page spacing</name>
  <files>frontend/src/pages/Dashboard.tsx, frontend/src/components/dashboard/StatsPanel.tsx</files>
  <action>
In Dashboard.tsx, normalize the spacing so all content sections use consistent gaps:

1. The root div currently has `pb-20`. Keep this for FAB clearance.
2. The content sections after HeroSection currently use individual `px-4 mt-4` wrappers. Wrap all non-fixed content (RoastWelcomeCard through HabitList) in a `space-y-4` container with `px-4` to create uniform vertical rhythm:

```tsx
<div className="pb-20">
  {/* Sticky mini hero when hero scrolls out */}
  {!heroVisible && (
    <div className="fixed top-0 left-0 right-0 z-30 transition-all duration-300">
      <MiniHero />
    </div>
  )}

  {/* Sentinel for IntersectionObserver */}
  <div ref={sentinelRef} className="h-px" />

  {/* Roast/Welcome Card */}
  <RoastWelcomeCard />

  {/* Hero Section */}
  <HeroSection />

  {/* Content with consistent spacing */}
  <div className="px-4 space-y-4 mt-4">
    <StatsPanel />
    {todayHabits.length === 0 ? (
      <EmptyState onCreateClick={() => openModal('habit-create')} />
    ) : (
      <HabitList />
    )}
  </div>

  {/* FAB, sheets, nudge banner remain unchanged */}
  ...
</div>
```

This removes the individual `px-4 mt-4` divs around StatsPanel and HabitList and uses a single consistent `space-y-4` container.

No changes to Analytics.tsx or Settings.tsx — they already have consistent `p-4 space-y-6` / `p-4 space-y-4` patterns.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Dashboard content sections wrapped in single px-4 space-y-4 container. No individual mt-4 wrappers around StatsPanel and HabitList. All existing tests pass.</done>
</task>

</tasks>

<verification>
- [ ] Load app at 1440px viewport width — content is centered, max 768px wide
- [ ] BottomTabBar spans full viewport width (not constrained by container)
- [ ] FAB button is positioned relative to viewport (not constrained by container)
- [ ] Animation overlays cover full viewport (not constrained by container)
- [ ] Dashboard has uniform spacing between sections
- [ ] Analytics and Settings pages render correctly within container
- [ ] All existing tests pass
</verification>

<success_criteria>
- Content centered at max-width 768px on 1440px viewport
- Uniform gap spacing on dashboard (no inconsistent mt-4 wrappers)
- Fixed-position elements (BottomTabBar, FAB, overlays) unaffected by container
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-polish-responsive-design/18-01-SUMMARY.md`
</output>
