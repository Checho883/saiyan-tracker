---
phase: 25-core-visual-assets
task: 2
total_tasks: 2
status: verification_pending
last_updated: 2026-03-13T20:42:18.418Z
---

<current_state>
Phase 25 execution is DONE (both plans complete). Verification ran and returned `human_needed`.
The user approved the avatar files visually but we never completed the phase-close steps
(marking phase complete in ROADMAP.md, updating STATE.md, offering next phase).

Phase 26 (Component Art Integration) is the only phase left in v2.0 but it's cosmetic polish.
The app is functionally complete and deployable NOW.

The user's real priority is: GET THE APP RUNNING so they can use it.
They need step-by-step deployment instructions for their Hostinger VPS + Vercel.
</current_state>

<completed_work>

- Plan 25-01: Source & process art assets — ✓ Done (3 commits: 3d818a3, 79f787e, 8d8dccc)
  - 9 WebP avatar files in frontend/public/assets/avatars/
  - Processing script kept at frontend/scripts/process-avatars.mjs for future re-runs
  - User downloaded 93 raw images + 7 videos (wants rotating images + video support later)
- Plan 25-02: Backend avatar_path updates — ✓ Done (prior session)
- Verification: ran, status `human_needed`, user approved avatars visually
</completed_work>

<remaining_work>

1. CLOSE PHASE 25: Run `phase complete` to mark ROADMAP.md and STATE.md done
2. PHASE 26 (optional): Component Art Integration — Shenron SVG, Dragon Ball orbs, Capsule Corp art, background theme. User may want to skip/defer this.
3. DEPLOYMENT (user's real priority):
   - VPS (Hostinger): SSH in, install packages, create saiyan user, clone repo, set up Python venv, create /etc/saiyan-tracker.env, install systemd service, configure Nginx, get TLS cert with certbot
   - Frontend (Vercel): Import repo, set root to frontend/, add VITE_API_BASE env var, deploy
   - Full guide exists at deploy/SETUP.md
   - Config files ready: deploy/saiyan-tracker.service, deploy/saiyan-tracker.nginx, deploy/saiyan-tracker.env.example, deploy/deploy.sh, deploy/smoke-test.sh
4. FUTURE FEATURE: User wants rotating images (multiple per character slot) + video/animation support — plan as a new phase after v2.0
</remaining_work>

<decisions_made>

- Finish Phase 25 as planned (1 image per slot) — rotating images + video support deferred to future phase
- User picked specific images from their 93 downloads for each avatar slot
- Processing script (sharp + process-avatars.mjs) kept for future re-processing with different image selections
- Phase 26 is cosmetic polish — not blocking deployment or app usage
- App has been functionally complete since v1.3 (Phase 22) — v2.0 phases are deployment + visual polish
</decisions_made>

<blockers>

- User needs a domain name pointed at VPS IP for certbot TLS setup
- User needs to know their VPS SSH credentials and OS details
- Unknown: has user already set up anything on the VPS? (they mentioned "I just checked my vps")
</blockers>

<context>
The user is eager to start USING their habit tracker. They've been building for days and want
to see it live. Tomorrow's session should:

1. First — close out Phase 25 (quick: mark complete in roadmap/state)
2. Then — walk them through deployment step by step, interactively
   - Ask what OS their VPS runs, if they have a domain, SSH access etc.
   - Guide them through each VPS step, waiting for confirmation
   - Then set up Vercel
3. Phase 26 can wait — or be skipped entirely

The deploy/ directory has everything needed. Key files:
- deploy/SETUP.md — full setup guide
- deploy/saiyan-tracker.service — systemd unit
- deploy/saiyan-tracker.nginx — nginx config
- deploy/saiyan-tracker.env.example — env template
- deploy/deploy.sh — auto-deploy script
- deploy/smoke-test.sh — post-deploy verification

For local testing (if they want to try before deploying):
- Terminal 1: cd backend && venv/Scripts/activate && uvicorn app.main:app --reload
- Terminal 2: cd frontend && npm install && npm run dev
- Open http://localhost:5173
</context>

<next_action>
Start with: Close Phase 25 (run phase complete), then ask user about their VPS setup
(OS, domain, SSH access) and walk them through deployment step by step.
Run /gsd:resume-work to pick up from here.
</next_action>
