---
phase: 25-core-visual-assets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/habits.py
  - backend/app/api/v1/quotes.py
  - backend/app/services/roast_service.py
  - backend/tests/test_api_quotes.py
  - backend/tests/test_api_habits.py
  - backend/tests/test_roast_service.py
autonomous: true
requirements: [ART-10]

must_haves:
  truths:
    - Backend avatar_path fields return /assets/avatars/{character}.webp (not /avatars/{character}.png)
    - All 5 hardcoded avatar_path constructions use the new path pattern
    - Existing backend tests pass with updated paths
  artifacts:
    - backend/app/api/v1/habits.py (updated avatar_path)
    - backend/app/api/v1/quotes.py (updated avatar_path)
    - backend/app/services/roast_service.py (updated avatar_path)
  key_links:
    - CharacterQuote.tsx uses avatar_path from API to load character portraits
    - RoastWelcomeCard.tsx uses avatar_path from status API for Goku/Vegeta images
---

<objective>
Update all backend avatar_path constructions from the old `/avatars/{character}.png` pattern to `/assets/avatars/{character}.webp` to match the new asset file locations.

Purpose: Ensure backend API responses point to the correct WebP image paths that frontend components will load
Output: 5 path updates across 3 backend files, updated tests
</objective>

<execution_context>
@C:/Users/chech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-core-visual-assets/25-CONTEXT.md
@.planning/phases/25-core-visual-assets/25-RESEARCH.md
@backend/app/api/v1/quotes.py
@backend/app/api/v1/habits.py
@backend/app/services/roast_service.py
</context>

<interfaces>
<!-- All 5 locations follow identical pattern: f"/avatars/{quote.character}.png" → f"/assets/avatars/{quote.character}.webp" -->

From backend/app/api/v1/quotes.py (line 29):
  avatar_path=f"/avatars/{quote.character}.png"

From backend/app/api/v1/habits.py (line 91):
  avatar_path=f"/avatars/{quote.character}.png"

From backend/app/api/v1/habits.py (line 457):
  "avatar_path": f"/avatars/{milestone_quote.character}.png"

From backend/app/services/roast_service.py (line 108):
  "avatar_path": f"/avatars/{welcome_quote.character}.png"

From backend/app/services/roast_service.py (line 118):
  "avatar_path": f"/avatars/{roast_quote.character}.png"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update backend avatar_path to WebP paths and fix tests</name>
  <files>backend/app/api/v1/quotes.py, backend/app/api/v1/habits.py, backend/app/services/roast_service.py, backend/tests/test_api_quotes.py, backend/tests/test_api_habits.py, backend/tests/test_roast_service.py</files>
  <behavior>
    - Test: GET /api/v1/quotes/random returns avatar_path matching pattern /assets/avatars/{character}.webp
    - Test: POST habit check with milestone quote returns avatar_path matching /assets/avatars/{character}.webp
    - Test: Roast service welcome_back.avatar_path and roast.quote.avatar_path both use /assets/avatars/ prefix and .webp extension
    - Test: No remaining references to /avatars/*.png in any backend source file
  </behavior>
  <action>
1. **Update all 5 avatar_path constructions.** In each location, replace the old pattern with the new one:

   OLD: `f"/avatars/{quote.character}.png"` (or `{welcome_quote.character}`, `{roast_quote.character}`, `{milestone_quote.character}`)
   NEW: `f"/assets/avatars/{quote.character}.webp"` (matching variable name)

   Specific changes:
   - `backend/app/api/v1/quotes.py` line 29: `avatar_path=f"/assets/avatars/{quote.character}.webp"`
   - `backend/app/api/v1/habits.py` line 91: `avatar_path=f"/assets/avatars/{quote.character}.webp"`
   - `backend/app/api/v1/habits.py` line 457: `"avatar_path": f"/assets/avatars/{milestone_quote.character}.webp"`
   - `backend/app/services/roast_service.py` line 108: `"avatar_path": f"/assets/avatars/{welcome_quote.character}.webp"`
   - `backend/app/services/roast_service.py` line 118: `"avatar_path": f"/assets/avatars/{roast_quote.character}.webp"`

2. **Update existing tests** that assert on avatar_path values. Search test files for `/avatars/` + `.png` references and update to `/assets/avatars/` + `.webp`:
   - `backend/tests/test_api_quotes.py` — update expected avatar_path in response assertions
   - `backend/tests/test_api_habits.py` — update milestone quote avatar_path assertions
   - `backend/tests/test_roast_service.py` — update welcome/roast avatar_path assertions

3. **Verify no remaining old paths**: Run `grep -rn '"/avatars/' backend/app/` to confirm zero results.

4. **Run full backend test suite**: `cd backend && python -m pytest -x --tb=short`
  </action>
  <verify>
    <automated>cd backend && python -m pytest -x --tb=short</automated>
  </verify>
  <done>All 5 avatar_path constructions updated to /assets/avatars/{character}.webp, all backend tests pass, grep confirms no remaining /avatars/*.png references in backend/app/</done>
</task>

</tasks>

<verification>
- [ ] `grep -rn '"/avatars/' backend/app/` returns zero results (no old paths remain)
- [ ] `grep -rn '/assets/avatars/' backend/app/` returns exactly 5 results (all updated)
- [ ] `cd backend && python -m pytest` — full suite passes
- [ ] Quote API response includes avatar_path with `/assets/avatars/` prefix and `.webp` extension
</verification>

<success_criteria>
All 5 backend avatar_path constructions return `/assets/avatars/{character}.webp` instead of `/avatars/{character}.png`. Full backend test suite passes green. Zero remaining references to the old `/avatars/*.png` pattern in backend source code.
</success_criteria>

<output>
After completion, create `.planning/phases/25-core-visual-assets/25-02-SUMMARY.md`
</output>
